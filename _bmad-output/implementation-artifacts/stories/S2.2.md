# Story 2.2: Wybór slotu dostawy w checkoucie (lista dostępnych)

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

Jako **klient**,
chcę **wybrać w checkoucie termin dostawy z listy dostępnych slotów (wyliczonej serwerowo)**,
aby **wiedzieć, kiedy dojedzie zamówienie** (decyzje O1–O8, EPIC-2).

## Acceptance Criteria

1. **Lista slotów liczona SERWEROWO.** Strona koszyka (`src/app/(frontend)/[tenant]/koszyk/page.tsx`, server component) wylicza dostępne wystąpienia slotów przez **istniejącą** czystą funkcję `computeAvailableSlots` z `src/lib/delivery-slots.ts` (NIE pisać jej od nowa) i przekazuje wynik do `CartView` **propsami**. Klient nie liczy slotów po swojej stronie.
2. **Źródła danych (odczyt frontendowy, NFR1 — izolacja tenant):** nowy *plain server util* (NIE `'use server'`) czyta z `overrideAccess: true` + **ręcznym `where { tenant }`**:
   - kolekcję `delivery-slots` (S2.1) → mapuje na `DeliverySlot[]`,
   - kolekcję `delivery-date-exceptions` (S2.8) → mapuje na `DateException[]`.
   `now` = `new Date()` wstrzyknięte do `computeAvailableSlots`.
3. **`reservedCount` = `0` (świadomy placeholder w tej historii).** Zamówienia NIE mają jeszcze relacji `deliverySlot` (dodaje ją S2.4), więc recount aktywnych zamówień na slot jest niemożliwy. Przekaż `reservedCount: 0` dla każdego slotu. Realny recount z `orders` + wymuszanie capacity = **S2.7** (poza zakresem). Skutek w tej historii: filtr capacity wyklucza tylko sloty z `capacity <= 0`.
4. **Sekcja widoczna tylko, gdy tenant ma skonfigurowane okna (O8).** Util zwraca `deliveryEnabled` = (liczba rekordów `delivery-slots` tego tenanta > 0). Gdy `deliveryEnabled === false` → sekcja wyboru slotu **ukryta**, checkout działa jak dziś (bez slotu w payloadzie). Gdy `deliveryEnabled === true` → sekcja widoczna, wybór **wymagany** (O8).
5. **Tylko realne sloty na liście.** Sloty zamknięte (po cutoffie), przeszłe/rozpoczęte, z wyłączonych dat (O7) oraz z `capacity <= 0` są **niewidoczne** — gwarantuje to `computeAvailableSlots` (już przetestowane, 15 testów jednostkowych). Lista posortowana po dacie i godzinie (gwarantuje funkcja).
6. **Wybór jednego slotu i przekazanie do payloadu checkoutu.** Klient wybiera dokładnie jeden dostępny slot; wybór (identyfikator slotu **oraz** konkretna data wystąpienia `YYYY-MM-DD`) jest przekazywany do akcji `placeOrder`. W tej historii wartość jest **tylko przenoszona** w sygnaturze akcji (parametr opcjonalny). **Autorytatywna walidacja cutoffu = S2.3, capacity = S2.7, snapshot w zamówieniu = S2.4** — tu NIE walidujemy serwerowo i NIE zapisujemy do `orders`.
7. **Wymuszenie wyboru po stronie klienta (gdy `deliveryEnabled`).** Przy `deliveryEnabled === true` przycisk „Złóż zamówienie" jest zablokowany dopóki klient nie wybierze slotu. Gdy `deliveryEnabled === true`, ale lista dostępnych slotów jest pusta → komunikat PL „Brak dostępnych terminów dostawy — skontaktuj się z dostawcą." i zablokowany submit. (To client-side UX; twardy guard serwerowy to S2.3.)
8. **Granica hydratacji nienaruszona (NFR6, R-S2.5).** `CartView` nadal hydratuje się poprawnie; żaden nowy import nie wciąga `next/headers` do modułu czystego ani do komponentu klienckiego. `pnpm dev` startuje, checkout działa end-to-end (z oknami i bez okien), `pnpm lint` + `pnpm test` zielone bez regresji (w tym testy izolacji `orders-isolation`, `order-detail-idor`).

## Tasks / Subtasks

- [ ] **Task 1 — Plain server util odczytu slotów** (AC: 1, 2, 3, 4, 5)
  - [ ] Nowy plik `src/lib/delivery-slots-read.ts` (kebab-case, wzór: `src/lib/cart.ts` / `src/lib/addresses.ts`). **BEZ `'use server'`**, ale wolno importować `payload` + `config` (to server util, nie moduł czysty). Importuje `computeAvailableSlots`, `DeliverySlot`, `DateException`, `AvailableSlot` z `@/lib/delivery-slots`.
  - [ ] Eksport `getAvailableDelivery(tenantId: number): Promise<{ deliveryEnabled: boolean; slots: AvailableSlot[] }>`.
  - [ ] `payload.find({ collection: 'delivery-slots', overrideAccess: true, depth: 0, limit: 500, where: { tenant: { equals: tenantId } } })`. `deliveryEnabled = res.totalDocs > 0`.
  - [ ] `payload.find({ collection: 'delivery-date-exceptions', overrideAccess: true, depth: 0, limit: 500, where: { tenant: { equals: tenantId } } })` → `exceptions: DateException[]` (mapuj pole `date`).
  - [ ] Zmapuj dokumenty slotów na `DeliverySlot[]` ustawiając **`reservedCount: 0`** (AC#3). Pozostałe pola 1:1 z kolekcji (`weekday`, `windowStart`, `windowEnd`, `cutoffDaysBefore`, `cutoffTime`, `capacity`, `id`).
  - [ ] `const slots = computeAvailableSlots(mapped, exceptions, new Date())`. Zwróć `{ deliveryEnabled, slots }`.
  - [ ] ⚠️ Weryfikuj nazwy slugów kolekcji względem S2.1/S2.8 PO ich implementacji: slot = `delivery-slots`, wyjątki = `delivery-date-exceptions` (potwierdź w `payload.config.ts` / `payload-types.ts` — patrz Open Questions Q1).
- [ ] **Task 2 — Przekazanie slotów do `CartView`** (AC: 1, 4)
  - [ ] W `koszyk/page.tsx` wywołaj `getAvailableDelivery(tenant.id)` (obok istniejącego `getCustomerAddresses`). Przekaż `availableSlots={slots}` i `deliveryEnabled={deliveryEnabled}` jako propsy do `<CartView ... />`. `AvailableSlot` jest serializowalny (plain object) → bezpieczny na granicy server→client.
- [ ] **Task 3 — UI wyboru slotu w `CartView`** (AC: 5, 6, 7)
  - [ ] Rozszerz propsy `CartView` o `availableSlots?: AvailableSlot[]` i `deliveryEnabled?: boolean` (import typu `AvailableSlot` z `@/lib/delivery-slots` — to typ, nie I/O; bezpieczny dla klienta).
  - [ ] Dodaj stan `selectedSlot: { id: AvailableSlot['id']; date: string } | null` (`useState`).
  - [ ] Renderuj sekcję „Termin dostawy" TYLKO gdy `deliveryEnabled`. W środku `<select>` (wzór: istniejący `saved-address` select w tym pliku) z opcjami z `availableSlots`. **Value opcji koduje id + datę** (np. `` `${slot.id}__${slot.date}` ``); w `onChange` parsuj z powrotem do `{ id, date }`.
  - [ ] Etykieta opcji PL: dzień tygodnia + data + okno, np. „pon. 23.06.2026, 08:00–12:00". Nazwę dnia weź z `weekday` (0–6, 0=niedziela) przez stałą tablicę PL; datę sformatuj z `date` (`YYYY-MM-DD`). Można dodać `({remaining} miejsc)` jeśli sensowne (opcjonalne).
  - [ ] Gdy `deliveryEnabled && availableSlots.length === 0` → komunikat PL „Brak dostępnych terminów dostawy — skontaktuj się z dostawcą." zamiast selecta.
  - [ ] `disabled` na przycisku submit: dotychczasowe `belowMin || submitting` **plus** `(deliveryEnabled && !selectedSlot)` (AC#7).
- [ ] **Task 4 — Przekazanie wyboru do `placeOrder`** (AC: 6)
  - [ ] W `src/app/(frontend)/[tenant]/actions.ts` rozszerz sygnaturę `placeOrder` o **opcjonalny** parametr przenoszący wybór, np. `deliverySlot?: { id: number; date: string }` (typ inline — NIE importować z modułu z `next/headers`; trzymać typy akcji inline zgodnie z gotchą). W tej historii parametr jest **tylko przyjmowany** (bez walidacji/zapisu) — przygotowanie gruntu pod S2.3/S2.4.
  - [ ] W `CartView.onSubmit` przekaż `selectedSlot` do `placeOrder(tenantId, contact, selectedSlot ?? undefined)`.
  - [ ] NIE dodawać pola do `payload.create({ collection: 'orders' })` — relacja `deliverySlot` nie istnieje (S2.4). NIE re-walidować cutoffu/capacity (S2.3/S2.7).
- [ ] **Task 5 — Test integracyjny odczytu + izolacji** (AC: 2, 4) — *zalecane, wzór: `tests/integration/*.integration.test.ts`*
  - [ ] Utwórz sloty dla tenanta A i B przez Local API (stempluj `tenant` jawnie). Potwierdź `getAvailableDelivery(A)` zwraca tylko sloty A (`deliveryEnabled` true; brak wycieku cross-tenant — NFR1).
  - [ ] Tenant bez slotów → `deliveryEnabled === false`, `slots === []` (O8).
  - [ ] (Opcjonalnie) data dodana do `delivery-date-exceptions` tenanta A wyklucza wystąpienia tego dnia (smoke; pełne pokrycie logiki już w `tests/unit/delivery-slots.test.ts`).
- [ ] **Task 6 — Weryfikacja końcowa** (AC: 8)
  - [ ] `pnpm dev`: checkout u tenanta z oknami pokazuje sekcję i listę; u tenanta bez okien sekcja ukryta; **`CartView` hydratuje się** (klik +/- ilości, wybór slotu, submit działają — brak cichej awarii wyspy).
  - [ ] `pnpm lint:fix` + `pnpm lint` czyste (Biome). `pnpm payload:types` jeśli dotykasz typów kolekcji (tu raczej nie).
  - [ ] `pnpm test` (live Docker Postgres `od-sasiada-pg`) zielone, zero regresji (w tym `orders-isolation`, `order-detail-idor`).
  - [ ] `fallow audit --format json --quiet` bez nowych blokad przed commitem.

## Dev Notes

### Co to za historia (zakres ścisły)
S2.2 to **wyłącznie ścieżka odczytu + UI wyboru**: serwerowy odczyt slotów/wyjątków → `computeAvailableSlots` → propsy → picker w `CartView` → przeniesienie wyboru do akcji. **Poza zakresem (świadomie, kolejne historie):**
- **S2.3** — serwerowa walidacja cutoffu w `placeOrder` (klientowi nie ufamy).
- **S2.7** — recount z `orders` + capacity odporne na wyścig (transakcja, `SELECT … FOR UPDATE`).
- **S2.4** — snapshot slotu w zamówieniu (relacja `deliverySlot` na `orders` + widok w panelu/froncie/mailu).
- **S2.6** — PL-izacja treści maili.

Nie zapisujemy slotu do `orders` (brak pola) i nie re-walidujemy go serwerowo w tej historii — wartość jest tylko **przenoszona**, by S2.3/S2.4 miały na czym budować.

### Fundament JUŻ ISTNIEJE — nie wymyślać od nowa
- **`computeAvailableSlots(slots, exceptions, now, horizonDays?)`** w `src/lib/delivery-slots.ts:150` — czysta, deterministyczna, przetestowana (DST, cutoff, O7, O4, O8). To **jedyne źródło prawdy** „które sloty są dostępne". Wstrzykujesz `now`; reszta filtrowania (przeszłe/cutoff/wyłączone/pełne/zdegenerowane) dzieje się w środku. **NIE duplikować tej logiki w UI ani w utilu odczytu.**
- Typy do importu z tego samego modułu: `DeliverySlot`, `DateException`, `AvailableSlot`, `Weekday`.

### Granica hydratacji Turbopack (R-S2.5 / NFR6) — KRYTYCZNE
Architektura warstw jest celowa i powtarza sprawdzony wzór z koszyka/adresów:
- **Moduł czysty** (`delivery-slots.ts`) — bez `'use server'`, `next/headers`, `@/lib/auth`, Reacta, I/O. Już taki jest; **nie dotykać tej zasady.**
- **Plain server util** (`delivery-slots-read.ts`, NOWY) — importuje `payload`/`config`, ale **bez `'use server'`**. Wzór 1:1: `src/lib/cart.ts` (`getCartSnapshot`) i `src/lib/addresses.ts` (`getCustomerAddresses`). Importowany **tylko** z server-componentu (`koszyk/page.tsx`), NIGDY z `CartView` (client).
- **Plik akcji** (`actions.ts`, `'use server'`) — importuje `next/headers` **bezpośrednio**, typy parametrów **inline**. Nie importować tego pliku jednocześnie z server- i client-componentu. (To już respektowane — zachować przy dodaniu parametru.)
- **Client component** (`CartView`) — dostaje `availableSlots`/`deliveryEnabled` propsami; importuje z `@/lib/delivery-slots` **tylko typ** `AvailableSlot` (typy znikają w kompilacji, bezpieczne). NIE wywołuje utila odczytu.

Złamanie którejkolwiek warstwy = **cicha awaria hydratacji wyspy klienckiej** (bez błędu w konsoli). Patrz memory `use-server-turbopack-gotcha` i nagłówek `delivery-slots.ts:6`.

### Wzór odczytu frontendowego (NFR1 — izolacja tenant)
Każdy odczyt na froncie idzie przez `overrideAccess: true` **z ręcznym `where { tenant }`** — plugin multi-tenant NIE pilnuje publicznych zapytań (pilnuje panelu/zapisu). Wzór: `getCatalog` w `src/lib/shop.ts:38` i `getCartSnapshot` w `src/lib/cart.ts:35`. **Brak filtra `tenant` = wyciek cross-tenant.** Tu dotyczy obu odczytów: `delivery-slots` i `delivery-date-exceptions`.

### Mapowanie dokumentów na `DeliverySlot`
Pola kolekcji `delivery-slots` (z S2.1, kontrakt SPIKE-S2) mapują się 1:1 na typ `DeliverySlot` (`delivery-slots.ts:29`). **Jedyna różnica:** `reservedCount` NIE jest polem kolekcji — to argument wejściowy funkcji. W tej historii ustaw `reservedCount: 0` (AC#3). Gdy `capacity` przyjdzie jako `number | null/undefined` z bazy — zabezpiecz (`?? 0`), bo funkcja robi `Math.floor`.

### Kodowanie wyboru: potrzebne ID + DATA
Jeden cykliczny slot (`id`) generuje wiele wystąpień (różne `date`). Do późniejszej walidacji (S2.3) i snapshotu (S2.4) `placeOrder` musi znać **oba**: `id` slotu i konkretną `date` (`YYYY-MM-DD`). Dlatego value opcji selecta koduje parę (`` `${id}__${date}` ``), a stan/parametr akcji to `{ id, date }`. Nie wystarczy samo `id`.

### Etykieta dnia tygodnia (PL)
`AvailableSlot.weekday` to `0–6` wg JS `getDay()` (**0 = niedziela**). Stała tablica PL: `['niedz.','pon.','wt.','śr.','czw.','pt.','sob.']` indeksowana `weekday`. Datę formatuj prosto z `date` (`YYYY-MM-DD` → `DD.MM.YYYY` lub `DD.MM`). Trzymaj formatowanie lekkie/inline w komponencie (lub mały helper) — bez nowych zależności dat.

### Stan UI i `disabled` submitu
Dotychczasowy guard: `disabled={belowMin || submitting}`. Dodaj człon dla slotu: gdy `deliveryEnabled` i brak `selectedSlot` → submit zablokowany (AC#7). Gdy `deliveryEnabled` i pusta lista → render komunikatu zamiast selecta + zablokowany submit. Gdy `!deliveryEnabled` → zero zmian w zachowaniu submitu (checkout jak dziś, O8).

### Środowisko / workflow
- **Postgres = Docker** (`od-sasiada-pg`, `postgres:17`, port 5432, baza `od_sasiada`) — NIE Homebrew. Jeśli daemon padł: `open -a Docker`, poczekaj aż `docker info` przejdzie, potem `docker start od-sasiada-pg`.
- **Biome** jedyny lint/format: single quotes, bez średników, `organizeImports`/`useSortedKeys`/`useSortedProperties` ON — pisz w dowolnej kolejności i odpal `pnpm lint:fix`.
- **Testy:** integracyjne `tests/integration/*.integration.test.ts` (live DB, `fileParallelism: false`, jedna instancja Payload, timeout 60s); unit `tests/unit/*.test.ts`. Alias `@/` działa w testach.
- Pieniądze (`remaining`/`capacity`) to liczniki miejsc, nie kwoty — nie dotyczy reguły groszy; ceny w `CartView` nadal przez `formatPLN`.

### Czego NIE robić (anti-patterns)
- ❌ Nie pisać `computeAvailableSlots` od nowa — istnieje i jest przetestowana (`delivery-slots.ts:150`).
- ❌ Nie liczyć/filtrować slotów po stronie klienta — `CartView` tylko renderuje gotową listę z propsów.
- ❌ Nie dawać `'use server'` ani `next/headers` modułowi `delivery-slots-read.ts` (jest plain server util) ani importować go z `CartView`.
- ❌ Nie czytać `delivery-slots`/`delivery-date-exceptions` bez `where { tenant }` (wyciek cross-tenant, NFR1).
- ❌ Nie liczyć `reservedCount` z `orders` (relacja `deliverySlot` nie istnieje — S2.4) → użyj `0`. Recount/capacity to S2.7.
- ❌ Nie zapisywać slotu do `orders` ani nie walidować cutoffu serwerowo w tej historii (S2.4/S2.3).
- ❌ Nie kodować wyboru samym `id` slotu (potrzebny też `date` wystąpienia).
- ❌ Nie filtrować `carts` po `status` (gotcha — nie dotyczy tej historii, ale pamiętaj).

### Project Structure Notes
- Czysty moduł: `src/lib/delivery-slots.ts` (istnieje). Reader: `src/lib/delivery-slots-read.ts` (NOWY, kebab-case — zgodne z `cart.ts`/`cart-validation.ts`/`addresses.ts`/`shop.ts`).
- UI: `src/components/shop/CartView.tsx` (istnieje, client). Akcja: `src/app/(frontend)/[tenant]/actions.ts` (`'use server'`, istnieje). Strona: `koszyk/page.tsx` (server, istnieje).
- Brak konfliktów ze strukturą. URL trasy po polsku (`koszyk`) zgodnie z konwencją.
- **Zależność blokująca:** kolekcje `delivery-slots` (S2.1) i `delivery-date-exceptions` (S2.8) muszą być zaimplementowane i obecne w `payload-types.ts` zanim ten reader się skompiluje (oba są `ready-for-dev`, build order: S2.1 → S2.8 → **S2.2**).

### References
- [Source: src/lib/delivery-slots.ts#150] — `computeAvailableSlots` (jedyne źródło prawdy dostępności); typy `DeliverySlot`/`DateException`/`AvailableSlot`/`Weekday` (#22,#29,#55,#63).
- [Source: _bmad-output/implementation-artifacts/stories/SPIKE-S2.md#(c)] — kontrakt funkcji, strefa Europe/Warsaw, DST, granica cutoffu `>=`, `reservedCount` jako wejście; #(d/e) recount z `orders` (S2.7); #43 pułapka hydratacji Turbopack.
- [Source: src/lib/cart.ts#28] — wzór plain server util (`getCartSnapshot`) + uzasadnienie granicy `'use server'`.
- [Source: src/lib/shop.ts#38] / [src/lib/addresses.ts] — wzór odczytu frontendowego z `overrideAccess: true` + ręczny `where { tenant }` (NFR1).
- [Source: src/components/shop/CartView.tsx#146] — istniejący wzór `<select>` (saved-address) i guard `disabled` submitu (#230).
- [Source: src/app/(frontend)/[tenant]/actions.ts#1,#74] — nagłówek gotchy hydratacji + sygnatura `placeOrder` do rozszerzenia (typy inline).
- [Source: src/app/(frontend)/[tenant]/koszyk/page.tsx] — miejsce wstrzyknięcia propsów.
- [Source: src/ecommerce/orders.ts] — orders NIE ma pola `deliverySlot` (potwierdza placeholder `reservedCount=0` i odłożenie zapisu do S2.4).
- [Source: _bmad-output/implementation-artifacts/stories/S2.1.md] / [S2.8.md] — kontrakt pól slotów i model wyjątków (`delivery-slots`, `delivery-date-exceptions`).
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-2] — O1–O8, kolejność budowania, zależność S2.1+S2.8.
- [Source: _bmad-output/project-context.md] — reguły TS/Payload/Biome/testów + granica `'use server'` + izolacja NFR1 (krytyczne).

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List

---

## Open Questions (do potwierdzenia przed/po dev)
- **Q1 — Slugi i nazwy pól kolekcji S2.1/S2.8.** Reader zakłada slug `delivery-slots` (S2.1, potwierdzony) i `delivery-date-exceptions` z polem `date` (S2.8 — w pliku historii pojawia się też wariant „lub analog"). **Przed implementacją Taska 1 potwierdź faktyczny slug i nazwę pola daty w zaimplementowanych S2.1/S2.8** (`payload.config.ts` / `payload-types.ts`). Jeśli S2.8 użył innego sluga — dostosuj `collection:` w `find`.
- **Q2 — Przenoszenie wyboru vs. wczesny guard serwerowy.** Rekomendacja: w S2.2 `placeOrder` tylko **przyjmuje** `deliverySlot` (bez walidacji), twardy guard cutoffu w S2.3. Alternatywa: dorzucić w S2.2 minimalny serwerowy „presence check" (jeśli `deliveryEnabled` i brak slotu → odrzuć). Domyślnie idziemy rekomendacją (czyste granice historii); potwierdź, jeśli wolisz wcześniejszy guard.
- **Q3 — Język etykiet UI.** Istniejący `CartView` ma część stringów po angielsku („Your cart", „Delivery details"), część po polsku. Nowa sekcja slotu = PL (zgodnie z regułą projektu „treść po polsku"). Pełna PL-izacja checkoutu nie jest w zakresie tej historii — potwierdź, czy zostawiamy spójność „PL tylko dla nowej sekcji".

_Ultimate context engine analysis completed — comprehensive developer guide created._
