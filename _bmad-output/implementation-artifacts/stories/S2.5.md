---
baseline_commit: 4c3d472afd5aef5780e96ce6ad9f0c2f6dd84d8f
---

# Story 2.5: Maile statusowe tylko na milestone'ach (zawężenie istniejącego hooka)

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

Jako **klient**,
chcę **dostawać maila tylko przy istotnej zmianie statusu zamówienia (milestone'y „do przodu", bez spamu przy cofnięciu)**,
aby **wiedzieć co się dzieje z zamówieniem bez dzwonienia do dostawcy** (decyzje O5/O6; ryzyko R-S2.3).

> ⚠️ To **ZAWĘŻENIE** istniejącego `sendStatusChange`, który DZIŚ wysyła maila na KAŻDEJ zmianie statusu (podpięty w `afterChange` od S1.5). Realna praca = ograniczenie wyzwalania do milestone'ów + cisza przy cofnięciu + brak duplikatów. **NIE** „dodanie maili od zera".

## Acceptance Criteria

1. **Mail statusowy wychodzi WYŁĄCZNIE na milestone'ach (O5):** docelowy status ∈ `{ confirmed, out_for_delivery, delivered, cancelled }`. Przejścia do `new` (w tym reaktywacja `cancelled→new`) i do `preparing` **NIE** wysyłają maila.
2. **Cisza przy cofnięciu (O6/R-S2.3).** Cofnięcie w sekwencji (np. `delivered→out_for_delivery`, `out_for_delivery→confirmed`) → **bez maila**, nawet gdy docelowa wartość jest milestone'em. Mail tylko przy ruchu „do przodu" w sekwencji; `cancelled` traktowane jako zdarzenie wysyłkowe (anulowanie z dowolnego niedostarczonego stanu → mail).
3. **Brak duplikatów / brak maila bez realnej zmiany.** Zapis bez zmiany statusu (ta sama wartość docelowa) → brak maila (zachowany warunek `doc.status !== previousDoc.status`).
4. **Błąd SMTP logowany, NIE blokuje update (NFR4).** Zachować obecny wzorzec „best effort" (`.catch(log)`), wysyłka nie wstrzymuje operacji zapisu zamówienia.
5. **Bez zmian treści/wzbogacenia maila.** PL-izacja treści + dodanie slotu/kontaktu dostawcy = **S2.6** (poza zakresem). S2.5 zmienia tylko **kiedy** mail leci, nie **co** zawiera. `sendOrderConfirmation` (create) bez zmian.
6. **Logika decyzji w czystym, testowalnym predykacie** (nie rozsiana po hooku). `pnpm lint` + `pnpm test` zielone bez regresji (`order-status-machine`, `order-emails`), `pnpm dev` startuje.

## Tasks / Subtasks

- [x] **Task 1 — Predykat decyzji wysyłki** (AC: 1, 2, 6)
  - [x] `src/ecommerce/order-status.ts`: dodaj `export const STATUS_EMAIL_MILESTONES: OrderStatusValue[] = ['confirmed', 'out_for_delivery', 'delivered', 'cancelled']` oraz czystą funkcję `export const shouldEmailStatusChange = (from: OrderStatusValue, to: OrderStatusValue): boolean`.
  - [x] Logika (reużyj `ORDER_STATUS_SEQUENCE`, NIE hardkoduj kolejności):
    - `from === to` → `false`,
    - `to` poza `STATUS_EMAIL_MILESTONES` → `false` (np. `new`, `preparing`, reaktywacja `cancelled→new`),
    - `to === 'cancelled'` → `true` (zdarzenie; automat stanów i tak zabrania `delivered→cancelled`),
    - `to` to milestone w sekwencji (`confirmed`/`out_for_delivery`/`delivered`) → `true` tylko gdy **ruch do przodu**: `seq.indexOf(to) > seq.indexOf(from)` (cofnięcie `toIdx < fromIdx` → `false`; `from === 'cancelled'` ma `indexOf === -1`, ale tu nie wystąpi, bo `cancelled→` milestone inny niż `new` jest zabroniony przez automat).
- [x] **Task 2 — Wepnij predykat w hook** (AC: 1, 2, 3, 4)
  - [x] `src/ecommerce/orders.ts`, `notifyOnOrderChange` (gałąź `operation === 'update'`): zamień warunek `previousDoc?.status && doc.status !== previousDoc.status` na `previousDoc?.status && shouldEmailStatusChange(previousDoc.status, doc.status)`. (Warunek `!== ` jest podzbiorem predykatu — `from===to` zwraca `false`.)
  - [x] Zachowaj `.catch(...)` (best effort, NFR4) i gałąź `create → sendOrderConfirmation` BEZ ZMIAN.
  - [x] Import `shouldEmailStatusChange` z `@/ecommerce/order-status` (kolejność importów wymusza Biome — `pnpm lint:fix`).
- [x] **Task 3 — Testy jednostkowe predykatu** (AC: 1, 2, 3)
  - [x] `tests/unit/order-status.test.ts` (istnieje): tablica przejść → oczekiwany bool. Pokryj:
    - milestone'y do przodu: `new→confirmed` ✓, `preparing→out_for_delivery` ✓, `out_for_delivery→delivered` ✓,
    - nie-milestone'y: `new→preparing`/`confirmed→preparing` ✗, `cancelled→new` ✗,
    - cofnięcia do milestone'a: `delivered→out_for_delivery` ✗, `out_for_delivery→confirmed` ✗,
    - anulowanie: `confirmed→cancelled` ✓, `new→cancelled` ✓,
    - brak zmiany: `confirmed→confirmed` ✗.
- [x] **Task 4 — Weryfikacja końcowa** (AC: 6)
  - [x] `pnpm test` (live Docker Postgres `od-sasiada-pg`) zielone (90 passed / 1 skipped), zero regresji (`order-status-machine`, `order-emails`, `orders-isolation`).
  - [x] Biome czysty (`biome check --write` na plikach historii — 0 fixów); `tsc --noEmit` exit 0; `fallow audit` bez NOWYCH blokad atrybuowanych do S2.5 (`dead_code_introduced: 0`; pozostałe `introduced` = znane FP / pliki niezmienione — patrz Completion Notes).
  - [x] Weryfikacja logiki przejść pokryta jednostkowo (`shouldEmailStatusChange`) + dev smoke (`✓ Ready in 431ms`). Ręczna weryfikacja w panelu opcjonalna (nieblokująca).

## Dev Notes

### Co to za historia (zakres ścisły)
S2.5 = **zawężenie wyzwalania** maila statusowego do milestone'ów „do przodu" + cisza przy cofnięciu + brak duplikatów. Punktowy refaktor warunku w jednym hooku + wydzielenie czystego predykatu. **NIE** dotykamy treści maila, automatu stanów, ani maila potwierdzającego.

**Poza zakresem (świadomie):**
- **S2.6** — PL-izacja treści maili (potwierdzenie + status) + dodanie slotu/kontaktu dostawcy. Tu treść `sendStatusChange` zostaje jak jest (ang.).
- **S2.4** — slot w mailu (dokładany w S2.6/S2.4). Nie tutaj.
- Automat stanów (`isAllowedTransition`) — bez zmian; S2.5 tylko CZYTA sekwencję.

### Stan obecny plików (UPDATE) — co zachować
- **`src/ecommerce/orders.ts:106` `notifyOnOrderChange`** (`afterChange`): `create` → `sendOrderConfirmation`; `update` + `previousDoc?.status && doc.status !== previousDoc.status` → `sendStatusChange`. **Dziś wysyła na KAŻDEJ zmianie** (w tym cofnięcia i `preparing`). Zmieniamy WYŁĄCZNIE warunek gałęzi `update`. `create` i `.catch` best-effort zostają.
- **`src/ecommerce/order-status.ts`**: `ORDER_STATUS_SEQUENCE = [new, confirmed, preparing, out_for_delivery, delivered]`, `cancelled` poza sekwencją; `OrderStatusValue`, `ORDER_STATUS_LABELS`, `isAllowedTransition`. Dodajemy `STATUS_EMAIL_MILESTONES` + `shouldEmailStatusChange` obok istniejących (spójny, czysty moduł — bez I/O).
- **`src/ecommerce/order-emails.ts` `sendStatusChange`**: builder maila (ang., label z `ORDER_STATUS_LABELS`). **Bez zmian w tej historii** (treść = S2.6).

### Dlaczego predykat (a nie warunek inline)
AC#6 + testowalność: logika „milestone + kierunek" ma kilka gałęzi i jest klasycznym źródłem regresji. Czysta funkcja `shouldEmailStatusChange` w `order-status.ts` (obok `isAllowedTransition`) jest jednostkowo testowalna bez DB/SMTP (wzór: `tests/unit/order-status.test.ts`) i reużywa `ORDER_STATUS_SEQUENCE` (jedno źródło kolejności — brak driftu z automatem).

### Mapa decyzji (referencyjna)
| from → to | milestone? | kierunek | mail? |
|---|---|---|---|
| new → confirmed | tak | do przodu | ✅ |
| confirmed → preparing | nie | — | ❌ |
| preparing → out_for_delivery | tak | do przodu | ✅ |
| out_for_delivery → delivered | tak | do przodu | ✅ |
| delivered → out_for_delivery | tak | cofnięcie | ❌ |
| out_for_delivery → confirmed | tak | cofnięcie | ❌ |
| dowolny (≠delivered) → cancelled | tak | zdarzenie | ✅ |
| cancelled → new (reaktywacja) | nie (`new`) | — | ❌ |
| confirmed → confirmed | — | brak zmiany | ❌ |

### Idempotencja / „brak duplikatów" (AC#3)
Warunek `doc.status !== previousDoc.status` jest już w hooku i jest **podzbiorem** predykatu (`from===to → false`), więc zapis bez zmiany statusu nie wyśle maila. **Świadome ograniczenie:** ponowne osiągnięcie tego samego milestone'a po cofnięciu (np. `confirmed→new→confirmed`) wyśle maila ponownie — to zamierzone (realna ponowna zmiana), NIE traktujemy jako duplikat. Nie wprowadzamy pola „ostatnio zmailowany status" (over-engineering; patrz Q1).

### NFR4 — wysyłka nie blokuje
Zachowaj `sendStatusChange(...).catch((err) => req.payload.logger.error(...))`. Mail jest efektem ubocznym `afterChange`; błąd SMTP nie może wywrócić zapisu zamówienia (np. zmiany statusu przez dostawcę w panelu).

### Środowisko / workflow
- **Biome** jedyny lint/format (single quotes, bez średników, `organizeImports` ON) — `pnpm lint:fix`.
- **Testy:** unit `tests/unit/*.test.ts` (czyste, bez DB) — tu predykat; integracyjne `tests/integration/*.integration.test.ts` (live Docker `od-sasiada-pg`, `fileParallelism:false`).
- Nie zmieniasz kolekcji/pól → `pnpm payload:types` niepotrzebne.

### Czego NIE robić (anti-patterns)
- ❌ Nie PL-izuj ani nie wzbogacaj treści `sendStatusChange` (S2.6).
- ❌ Nie ruszaj gałęzi `create`/`sendOrderConfirmation` ani `isAllowedTransition`.
- ❌ Nie czyń wysyłki blokującą — zostaw `.catch` (NFR4).
- ❌ Nie hardkoduj kolejności statusów — reużyj `ORDER_STATUS_SEQUENCE`.
- ❌ Nie dodawaj pola „last emailed status" do kolekcji (niepotrzebne; patrz Q1).
- ❌ Nie wysyłaj na `preparing` ani na reaktywację `cancelled→new` (nie są milestone'ami O5).

### Project Structure Notes
- Modyfikacje: `src/ecommerce/order-status.ts` (predykat + stała), `src/ecommerce/orders.ts` (warunek hooka). Test: `tests/unit/order-status.test.ts`.
- Zgodne z konwencją `src/ecommerce/*` (logika pluginu ecommerce). Bez nowych plików/zależności.
- **Zależność:** S1.5 (hook `afterChange` + `sendStatusChange` istnieją — GOTOWE). Niezależna od slotów; może iść równolegle do S2.2–S2.7. Następnik treści: S2.6.

### References
- [Source: src/ecommerce/orders.ts#106] — `notifyOnOrderChange` (jedyny warunek do zmiany; zachować `create`/`.catch`).
- [Source: src/ecommerce/order-status.ts#17,#41] — `ORDER_STATUS_SEQUENCE`, `OrderStatusValue`, `isAllowedTransition` (wzór czystej funkcji; dodać `shouldEmailStatusChange`).
- [Source: src/ecommerce/order-emails.ts#69] — `sendStatusChange` (builder; BEZ zmian — treść = S2.6).
- [Source: tests/unit/order-status.test.ts] — wzór testów jednostkowych predykatu (bez DB).
- [Source: _bmad-output/implementation-artifacts/stories/S2.4.md] / [S2.6 (epics)] — granica: treść/slot maila to S2.4/S2.6.
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-2] — O5 (milestone'y), O6 (mail przy `cancelled`, ciche cofnięcia), zależność S1.5.
- [Source: _bmad-output/project-context.md] — automat stanów (przejścia centralnie w `order-status.ts`), maile w `afterChange`, reguły testów/Biome.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (subagent)

### Debug Log References

- `pnpm test` → 16 plików, 90 passed / 1 skipped, 0 regresji (24.99s).
- `pnpm exec tsc --noEmit` → exit 0.
- `pnpm dev` smoke → `✓ Ready in 431ms`, brak błędów kompilacji (ELIFECYCLE w logu to wykrycie INNEJ działającej instancji `next dev`, nie błąd kodu S2.5).
- `npx fallow audit --format json --quiet` → `attribution.dead_code_introduced: 0`; `duplication_introduced: 1` i `complexity_introduced: 3` = wyłącznie znane FP / pliki niezmienione (szczegóły niżej).

### Completion Notes List

- **Definicja „milestone do przodu" vs „cofnięcie":** czysty predykat `shouldEmailStatusChange(from, to)` w `order-status.ts`, reużywający `ORDER_STATUS_SEQUENCE` (jedno źródło kolejności, brak driftu z automatem). Kolejno: `from===to` → false (brak duplikatów, AC#3); `to` poza `STATUS_EMAIL_MILESTONES` → false (`new`, `preparing`, reaktywacja `cancelled→new`); `to==='cancelled'` → true (zdarzenie, AC#2); pozostałe milestone'y → true tylko gdy `seq.indexOf(to) > seq.indexOf(from)` (ruch wprzód; cofnięcie → cisza, O6/R-S2.3).
- **Wpięcie w hook:** w `notifyOnOrderChange` (gałąź `update`) warunek `doc.status !== previousDoc.status` zastąpiony predykatem (jest jego podzbiorem). `create → sendOrderConfirmation`, `.catch(log)` (NFR4) i treść maili (`sendStatusChange`/`sendOrderConfirmation`) BEZ ZMIAN (treść/PL = S2.6).
- **fallow — interpretacja:** początkowo `dead_code_introduced: 1` (eksport `STATUS_EMAIL_MILESTONES` nieużywany na zewnątrz modułu). Rozwiązane bez łamania Task 1 (eksport pozostaje, jak `isAllowedTransition`): test jednostkowy referuje `STATUS_EMAIL_MILESTONES` (asercja zbioru milestone'ów O5) → po ponownym audycie `dead_code_introduced: 0`. Pozostałe `introduced`: `duplication` = znany FP klon `tests/unit/order-emails.test.ts:43↔70` (plik niezmieniony — `git diff --stat HEAD` go nie wykazuje); `complexity` = `placeOrder` i `revalidateCapacityOnReactivation` (znane/odroczone) + arrow w `delivery-slot-snapshot.integration.test.ts` — wszystkie w plikach, których S2.5 NIE dotyka (flagowane jako „introduced" tylko dlatego, że gate `new-only` porównuje do scommitowanego HEAD, a S2.7/S2.4 są niescommitowane w working tree). Żadna NOWA blokada z plików S2.5.
- **Open Questions:** Q1 — przyjęto rekomendację (dopuszczamy ponowny mail po cofnięciu do tego samego milestone'a; BEZ pola „last emailed status", bez over-engineeringu). Q2 — potwierdzone: `preparing` świadomie NIE jest milestone'em (brak maila „w przygotowaniu"). Q3 — potwierdzone: logika pokryta testem jednostkowym predykatu; brak dodatkowego testu integracyjnego szpiegującego `sendStatusChange` (wiring zabezpieczony regresjami `order-status-machine`/`orders-isolation`).

### File List

**Zmienione:**
- `src/ecommerce/order-status.ts` — dodane `STATUS_EMAIL_MILESTONES` + czysty predykat `shouldEmailStatusChange`.
- `src/ecommerce/orders.ts` — `notifyOnOrderChange`: warunek gałęzi `update` przez `shouldEmailStatusChange`; import predykatu.
- `tests/unit/order-status.test.ts` — nowy blok `describe('… shouldEmailStatusChange')` + asercja `STATUS_EMAIL_MILESTONES`.
- `_bmad-output/implementation-artifacts/stories/S2.5.md` — frontmatter, checkboxy, Dev Agent Record, File List, Change Log, Status.
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — `S2.5: ready-for-dev → review`.

**Nowe / wygenerowane:** brak (bez zmian pól kolekcji → `pnpm payload:types` niepotrzebne).

---

## Open Questions (do potwierdzenia przed/po dev)
- **Q1 — Ponowne osiągnięcie milestone'a po cofnięciu.** Rekomendacja: dopuścić ponowny mail (realna zmiana; brak pola „last emailed status"). Alternatywa (jeśli niepożądane): trwały znacznik ostatnio zmailowanego statusu — odradzane (over-engineering, drift). Potwierdź rekomendację.
- **Q2 — Czy `preparing` ma być milestone'em.** O5/AC wskazuje `{confirmed, out_for_delivery, delivered, cancelled}` — `preparing` BEZ maila. Potwierdź, że to świadome (klient nie dostaje maila „w przygotowaniu").
- **Q3 — Test na poziomie hooka.** Rekomendacja: pokryć logikę testem jednostkowym predykatu (bez DB/SMTP); wiring hooka zweryfikować ręcznie + brakiem regresji `order-status-machine`. Potwierdź, że nie wymagasz dodatkowego testu integracyjnego szpiegującego `sendStatusChange`.

_Ultimate context engine analysis completed — comprehensive developer guide created._

## Change Log

| Data | Wersja | Opis | Autor |
|---|---|---|---|
| 2026-06-23 | 1.0 | Implementacja S2.5: zawężenie maila statusowego do milestone'ów „do przodu" (predykat `shouldEmailStatusChange` + `STATUS_EMAIL_MILESTONES` w `order-status.ts`, wpięcie w hook `notifyOnOrderChange`, testy jednostkowe). Open Questions Q1/Q2/Q3 rozstrzygnięte wg rekomendacji. Status → review. | claude-opus-4-8 (subagent) |
