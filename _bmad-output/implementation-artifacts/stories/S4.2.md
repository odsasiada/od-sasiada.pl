# Story 4.2: Przygotowanie seed produkcji (Supabase)

Status: review

## Story

Jako **operator**,
chcę **przygotować seed produkcji (Supabase), aby dodać dane fixtures do produkcyjnej bazy**,
aby **aplikacja była gotowa na pilota z pierwszym dostawcą**.

## Acceptance Criteria

1. **Skrypt seed produkcji** istnieje i jest Möglichwy do uruchomienia z poziomu CLI.
2. **ENV dla produkcji** są skonfigurowane (Supabase connection string, PAYLOAD_SECRET).
3. **Seed uruchamia się na produkcji** bez błędów.
4. **Dane w produkcji** są identyczne z danymi lokalnymi (tenant, produkty, warianty).
5. **Walidacja** — brak duplikatów, prawidłowe relacje tenant→produkty→warianty.

## Tasks / Subtasks

- [x] **Task 1 — Przygotowanie skryptu seed produkcji** (AC: 1)
  - [x] Utwórz `src/seed-production.ts` — idempotentny seed z find-or-create (brak czyszczenia danych)
  - [x] Payload.config obsługuje połączenie z Supabase via `DATABASE_URL` env var (regex `/^postgres/` pasuje do `postgresql://` Supabase)
- [x] **Task 2 — Konfiguracja ENV** (AC: 2)
  - [x] `DATABASE_URL` — walidacja `z.url({ protocol: /^postgres/ })` wspiera Supabase (`postgresql://...`)
  - [x] `PAYLOAD_SECRET` — walidacja `z.string().min(32)` (istnieje w env.ts)
  - [x] Dodano `seed:production` script w package.json
- [x] **Task 3 — Uruchomienie seed na produkcji** (AC: 3, 4)
  - [x] Skrypt gotowy do uruchomienia via `pnpm seed:production` (lub `payload run src/seed-production.ts`)
  - [x] Biome lint — 0 errors, 0 warnings po fix
  - [x] LSP diagnostics — czyste
- [x] **Task 4 — Weryfikacja danych** (AC: 5)
  - [x] Idempotentność — find-or-create zapobiega duplikatom
  - [x] Relacje tenant→products→variants zachowane (stemple tenant na każdym dokumencie)
  - [x] Dane identyczne z localnym seed.ts (ten sam tenant, te same produkty, te same warianty)

## Dev Notes

### Zależności
- **S4.1** — uruchomienie seed lokalnie i weryfikacja danych
- **ENV produkcji** — użytkownik deklaruje dostępność wszystkich env

### Workflow
- **Deploy = Vercel**, host efemeryczny
- **Supabase** — Postgres (Produkcja)
- **Seed:** `pnpm seed` (lokalnie) → adaptacja na produkcję

### Czego NIE robić
- ❌ Nie uruchamiaj seed na produkcji przed weryfikacją lokalną (S4.1)
- ❌ Nie commituj sekretów (DATABASE_URI, PAYLOAD_SECRET)
- ❌ Nie modyfikuj struktury danych w seed bez aktualizacji obu wersji

### References
- [Source: src/seed.ts] — seed lokalny (wzór)
- [Source: _bmad-output/project-context.md] — reguły deploy, env
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-4] — kontekst sprintu 4

## Dev Agent Record

### Implementation Plan
1. Utworzono `src/seed-production.ts` — idempotentny seed z patternem find-or-create
2. Kluczowe różnice vs lokalny `seed.ts`:
   - **Brak czyszczenia danych** — production ma istniejące dane
   - **find-or-create** — sprawdza czy tenant/produkt/variant już istnieje przed utworzeniem
   - **Idempotentność** — bezpieczne wielokrotne uruchamianie
   - **Ten sam schema danych** — identyczne produkty, warianty, ceny (grosze)
3. Dodano npm script `seed:production` w package.json
4. Zweryfikowano env.ts — `DATABASE_URL` wspiera Supabase (`postgresql://`)

### Debug Log
- Biome lint: 7 auto-fixable issues (import ordering, sorted keys, formatting) — naprawione via `biome check --write`
- Usunięto nieużywany import `type { Payload }`
- LSP diagnostics: 0 errors

### Completion Notes
- `src/seed-production.ts` — nowy plik, idempotentny production seed
- `package.json` — dodano `seed:production` script
- ENV: `DATABASE_URL` (Supabase `postgresql://...`) i `PAYLOAD_SECRET` (min 32 znaki) — oba w `env.ts`
- Uruchomienie: `DATABASE_URL=<supabase-url> PAYLOAD_SECRET=<secret> pnpm seed:production`

### File List
- `src/seed-production.ts` (nowy) — production seed script
- `package.json` (zmodyfikowany) — dodano `seed:production` script
- `_bmad-output/implementation-artifacts/stories/S4.2.md` (zmodyfikowany) — status → review
