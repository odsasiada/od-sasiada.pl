---
baseline_commit: 90fae1387a9115ba0b4b1a54faff1091279a2271
---
# Story 3.2: Hero (zdjęcie) na produkcie ORAZ wariancie, z izolacją tenant

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

Jako **dostawca**,
chcę **przypisać pojedyncze zdjęcie hero do produktu oraz (osobno) do wariantu — oba opcjonalne**,
aby **prezentować właściwe zdjęcie, a izolacja tenant nie pozwalała wskazać cudzych mediów** (decyzja D3, D6; ryzyko R-S3.4).

## Acceptance Criteria

1. **Produkt ma pojedyncze, opcjonalne `heroImage`** → relacja `upload` do kolekcji `media`. Dodane przez `productsCollectionOverride` (istnieje w `payload.config.ts`) — zachować dotychczasowe pola (`title`, `description`, `...defaultCollection.fields`).
2. **Wariant ma własne, pojedyncze, opcjonalne `heroImage`** → relacja `upload` do `media`. Dodane przez **`variants.variantsCollectionOverride`** (NOWY klucz w configu pluginu ecommerce; dziś nieobecny) — `...defaultCollection.fields` zachować (m.in. `priceInPLN`, `title`, `options`).
3. **Tylko medium WŁASNEGO tenanta** (R-S3.4) — na OBU poziomach. Próba przypisania `media` innego tenanta jest **odrzucana serwerowo** (walidacja tenant-match przy zapisie, autorytatywna — nie tylko UI). Picker w panelu filtrowany do mediów tenanta (UX).
4. **Brak hero na obu poziomach dozwolony (D6)** — pola opcjonalne; zapis bez zdjęcia przechodzi.
5. **Fallback NIE tutaj.** Rozwiązywanie „wariant → produkt → placeholder" to **S3.3** (render na froncie). S3.2 dostarcza wyłącznie pola + izolację.
6. **`pnpm payload:types`** przechodzi (`heroImage` na typach `Product` i `Variant`), **`pnpm test`** zielone bez regresji, **`pnpm dev`** startuje, gotcha `products:` nietknięta.

## Tasks / Subtasks

- [x] **Task 1 — `heroImage` na produkcie** (AC: 1, 3, 4)
  - [x] `payload.config.ts`, `products.productsCollectionOverride`: dodano `heroImageField('Zdjęcie główne')` (`upload → media`, opcjonalne) przed `...defaultCollection.fields`; zachowane `title`, `description`, `...defaultCollection.fields`.
  - [x] `filterOptions` (UX) tenant-scoped: `data.tenant` → `{ tenant: { equals } }` (gdy brak tenanta → `true`). **Q1 rozstrzygnięte:** w `beforeValidate`-fields plugin stempluje `data.tenant` zanim wejdzie collection `beforeValidate`, więc `data.tenant` jest dostępne.
- [x] **Task 2 — `heroImage` na wariancie** (AC: 2, 3, 4)
  - [x] `payload.config.ts`: dodano `variants.variantsCollectionOverride` (`heroImageField('Zdjęcie wariantu')` + `...defaultCollection.fields`/`admin` zachowane). **Korekta vs treść taska:** `variants` to klucz `ProductsConfig` — **zagnieżdżony pod `products`**, nie top-level (potwierdzone w typach pluginu, linia 358).
  - [x] Analogiczny `filterOptions` tenant-scoped (ten sam `heroImageField`).
- [x] **Task 3 — Walidacja tenant-match (autorytatywna, R-S3.4)** (AC: 3)
  - [x] Wspólny `beforeValidate` `heroTenantMatch` w `src/ecommerce/hero-tenant-match.ts`, wpięty na produkcie i wariancie: gdy `heroImage` ustawione → `findByID('media', overrideAccess:true, disableErrors:true)` i odrzucenie gdy `media.tenant !== doc.tenant` (komunikat PL „Zdjęcie musi należeć do tego samego dostawcy."). Wzór z `cart-validation.ts`.
  - [x] Potwierdzone testem: hook działa nawet przy `overrideAccess:true` (overrideAccess omija access, NIE hooki) — `filterOptions` to tylko UI, twardy guard jest serwerowy.
- [x] **Task 4 — Regeneracja typów + sanity** (AC: 6)
  - [x] `pnpm payload:types` → `heroImage?: (number | null) | Media` na `Product` (#452) i `Variant` (#431). `payload-types.ts` nieedytowane ręcznie (generowane, gitignored).
  - [x] `pnpm dev` startuje — `✓ Ready in 411ms`, config sanitized OK (gotcha `products:` i nowy `variants` override nie wywalają sanitize). *Wizualny click-through panelu (picker tenant-scoped) to ręczny sanity UX — izolacja pokryta autorytatywnie testem; picker to wyłącznie UX.*
- [x] **Task 5 — Test integracyjny izolacji + opcjonalności** (AC: 3, 4) — `tests/integration/hero-tenant-isolation.integration.test.ts` (5 testów)
  - [x] Same-tenant: `media` A → produkt A i wariant A → OK.
  - [x] Cross-tenant: `media` B → produkt A i wariant A → **odrzucone** (oba poziomy).
  - [x] Bez hero → create produktu przechodzi (D6).
- [x] **Task 6 — Weryfikacja końcowa** (AC: 6)
  - [x] `pnpm test` zielone (18 plików, 104 passed / 1 skipped), zero regresji (`orders-isolation`, `media-isolation`); `pnpm lint:fix` + `pnpm lint` czyste (79 plików); `fallow audit` verdict `pass`, bez NOWYCH blokad (jedyny finding `@vercel/blob` prexisting z S3.1; duplikację setupu mediów usunięto refaktorem do wspólnego `fx.createMedia`).

## Dev Notes

### Co to za historia (zakres ścisły)
S3.2 = **dwa pola hero (produkt + wariant) + izolacja tenant**. NIE renderuje niczego na froncie i NIE rozwiązuje fallbacku — to S3.3. Tu liczy się: pola `upload → media`, opcjonalność (D6), i twardy tenant-match na obu poziomach (R-S3.4).

### Mechanizm override — POTWIERDZONY w pluginie
Plugin ecommerce wystawia `productsCollectionOverride` **oraz** `variantsCollectionOverride` (typ `CollectionOverride`, potwierdzone w `node_modules/@payloadcms/plugin-ecommerce/dist/types/index.d.ts`). Produkt JUŻ ma override (dodaje `title`/`description`) — rozszerz jego `fields`. Wariant NIE ma jeszcze override — dodaj klucz `variants: { variantsCollectionOverride }`. **Wzór 1:1 z `productsCollectionOverride`** (`payload.config.ts:138`): spread `...defaultCollection`, `fields: [...defaultCollection.fields, <nowe>]`, zachowaj `admin`.

### `upload` vs `relationship`
Użyj `type: 'upload', relationTo: 'media'` (nie `relationship`) — to idiomatyczne dla referencji do kolekcji Upload, daje w panelu picker z miniaturą/podglądem i drag&drop. Pojedyncze (bez `hasMany`) = jedno hero (D2/D3).

### Izolacja tenant (R-S3.4) — DWIE warstwy, walidacja obowiązkowa
`relationTo: 'media'` domyślnie może wskazać dokument **dowolnego** tenanta — multi-tenant plugin nie waliduje wartości pola relacji (pilnuje wierszy kolekcji, nie treści pól). Dlatego:
- **UI:** `filterOptions` ograniczające picker do mediów tenanta (wygoda, NIE zabezpieczenie).
- **Autorytatywnie:** `beforeValidate`/`validate` z odczytem `media` z DB i porównaniem `media.tenant === doc.tenant`. Bez tego Local API/zła wartość przejdzie → wyciek cross-tenant. To ta sama filozofia co `cart-validation.ts` (cena/tenant zawsze z DB, klientowi nie ufamy).
- `doc.tenant` jest dostępne, bo `products`/`variants` są w `multiTenantPlugin.collections` (plugin stempluje `tenant`).

### Fallback należy do S3.3 (granica)
D3 „wariant nadpisuje produkt; fallback wariant→produkt→placeholder" to logika **odczytu/renderu** — S3.3 (`getCatalog`/komponenty + `next/image`). S3.2 NIE implementuje wyboru zdjęcia do wyświetlenia; dostarcza tylko dwa niezależne pola. Nie wciągaj logiki prezentacji.

### Stan plików (UPDATE)
- **`payload.config.ts`**: `products.productsCollectionOverride` (`#138`) — rozszerz `fields`. Dodaj `variants: { variantsCollectionOverride }` w bloku `ecommercePlugin({...})`. **NIE ruszaj** `products: {...}` w sposób gubiący override (gotcha `ecommerce-plugin-products-gotcha`: brak `products` = crash sanitize relacji).
- **`src/collections/Media.ts`**: cel relacji (slug `media`, z S3.1) — bez zmian.
- Walidacja: nowy wspólny helper (np. `src/ecommerce/hero-tenant-match.ts` lub inline hook) — patrz Q2 (gdzie umieścić).

### Środowisko / workflow
- **Postgres = Docker** (`od-sasiada-pg`) — NIE Homebrew.
- **Po zmianie pól `pnpm payload:types`** (build robi automatycznie; lokalnie ręcznie). Nie edytować `payload-types.ts`/`importMap.js`.
- **Biome** (single quotes, bez średników, `organizeImports`/`useSortedKeys` ON) — `pnpm lint:fix`.
- **Testy:** integracyjne `tests/integration/*` (live DB, `fileParallelism:false`).

### Czego NIE robić (anti-patterns)
- ❌ Nie polegaj tylko na `filterOptions` (UI) — walidacja tenant-match serwerowa jest obowiązkowa (R-S3.4).
- ❌ Nie czyń `heroImage` wymaganym (D6 — opcjonalne na obu poziomach).
- ❌ Nie implementuj fallbacku wariant→produkt→placeholder (S3.3).
- ❌ Nie gub pól domyślnych przy override wariantu (`...defaultCollection.fields`) ani bloku `products:` (gotcha sanitize).
- ❌ Nie definiuj `tenant` ręcznie (plugin) i nie edytuj generowanych typów.
- ❌ Nie używaj `relationship` zamiast `upload` (gorszy UX, brak podglądu).

### Project Structure Notes
- Modyfikacje: `src/payload.config.ts` (oba override). Ewentualny helper walidacji: `src/ecommerce/*` (kebab/Pascal wg konwencji). Test: `tests/integration/hero-tenant-isolation.integration.test.ts`.
- **Zależność:** S3.1 (kolekcja `media` istnieje). Następnik: S3.3 (render + fallback). Build order: SPIKE-S3 → S3.1 → **S3.2** → S3.3.

### References
- [Source: node_modules/@payloadcms/plugin-ecommerce/dist/types/index.d.ts#288,#302,#348] — `variantsCollectionOverride`/`productsCollectionOverride` (typ `CollectionOverride`) — mechanizm dodania pól.
- [Source: src/payload.config.ts#138] — `productsCollectionOverride` (wzór spread `...defaultCollection` + `fields`); `#170` multi-tenant `media`/`variants`; gotcha `products:`.
- [Source: src/collections/Media.ts] — cel relacji `upload` (slug `media`, S3.1).
- [Source: src/lib/cart-validation.ts#41] — wzór autorytatywnej walidacji tenant-match „z DB, nie ufaj wejściu".
- [Source: tests/integration/orders-isolation.integration.test.ts] — wzór testu izolacji tenant.
- [Source: _bmad-output/implementation-artifacts/stories/SPIKE-S3.md] / [S3.1.md] — kolekcja `media`, izolacja R-S3.2/R-S3.4.
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-3] — D3 (hero produkt+wariant, fallback), D6 (opcjonalne), zależność S3.1.
- [Source: _bmad-output/project-context.md] — izolacja NFR1, gotcha `products:`, regen typów, reguły Biome/testów.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Claude Code dev-story)

### Debug Log References

- `pnpm payload:types` → `heroImage?: (number | null) | Media` na `Variant` (#431) i `Product` (#452).
- `pnpm test tests/integration/hero-tenant-isolation.integration.test.ts` → 5 passed.
- `pnpm test` (pełna) → 18 plików, 104 passed / 1 skipped, zero regresji.
- `pnpm lint:fix` + `pnpm lint` → czyste (79 plików).
- `npx fallow audit` → verdict `pass`. Pierwszy przebieg ujawnił NOWĄ duplikację (setup `createMedia`/sharp między `hero-` a `media-isolation`) → usunięta przez ekstrakcję wspólnego `fx.createMedia` do `tests/setup/fixtures.ts`; po refaktorze 0 clone groups. Jedyny pozostały finding: unused dep `@vercel/blob` (prexisting z S3.1, `introduced:false`).
- `pnpm dev` → `✓ Ready in 411ms` (config sanitized OK); exit 1 tylko z powodu zajętego portu 3000 przez istniejący serwer dev użytkownika — nie błąd kodu.

### Completion Notes List

- **Task 1/2 (AC1, AC2, AC4) — `heroImage` na produkcie i wariancie:** wspólny `heroImageField(label)` (`upload → media`, opcjonalny, `filterOptions` tenant-scoped) dodany w `productsCollectionOverride` oraz nowym `variants.variantsCollectionOverride`. Zachowane pola domyślne i `admin`.
- **Korekta strukturalna (vs treść Task 2):** `variants` NIE jest top-level kluczem pluginu — to pole `ProductsConfig` (`node_modules/@payloadcms/plugin-ecommerce/dist/types/index.d.ts#358`), więc `variants.variantsCollectionOverride` zagnieżdżone **pod `products`**. Wpięcie jako sibling by się nie typowało/nie zadziałało.
- **Task 3 (AC3, R-S3.4) — autorytatywna walidacja tenant-match:** `src/ecommerce/hero-tenant-match.ts` → `heroTenantMatch` (collection `beforeValidate`), reużywany przez oba override. Czyta `media` z DB (`overrideAccess:true, disableErrors:true`) i odrzuca, gdy `media.tenant !== doc.tenant` („Zdjęcie musi należeć do tego samego dostawcy."). **Q1/Q2 rozstrzygnięte:** zweryfikowano w źródłach Payload, że field `defaultValue` (stempel tenanta z cookie przez multi-tenant) jest aplikowany w fazie *beforeValidate-fields*, PRZED collection `beforeValidate` — więc `data.tenant` jest dostępne na create; `originalDoc.tenant` pokrywa update. Hook działa też przy `overrideAccess:true` (overrideAccess omija access, nie hooki).
- **Q3:** `upload` (picker z podglądem) zgodnie z rekomendacją.
- **Task 5 (AC3, AC4) — test:** `tests/integration/hero-tenant-isolation.integration.test.ts` (5): same-tenant produkt+wariant OK, cross-tenant produkt+wariant odrzucone, brak hero (D6) OK.
- **Refaktor testów (czystość/fallow):** wspólny `createMedia` (real 1200² PNG, tracked do cleanup) wyniesiony do `tests/setup/fixtures.ts`; `media-isolation` (S3.1) przepięty na `fx.createMedia` — usuwa duplikację wykrytą przez fallow, oba suity nadal zielone.
- **Granica zakresu:** fallback wariant→produkt→placeholder i render NIE są tutaj — to S3.3 (zgodnie z AC5).

### File List

- `src/ecommerce/hero-tenant-match.ts` (NEW) — `heroImageField` (factory) + `heroTenantMatch` (beforeValidate, R-S3.4).
- `src/payload.config.ts` (MOD) — import; `heroImage` + hook w `productsCollectionOverride`; nowy `products.variants.variantsCollectionOverride` z `heroImage` + hookiem.
- `tests/integration/hero-tenant-isolation.integration.test.ts` (NEW) — test izolacji + opcjonalności hero (5 przypadków).
- `tests/setup/fixtures.ts` (MOD) — wspólny `createMedia` (real PNG, tracked) + typ `TenantFixtures.createMedia`.
- `tests/integration/media-isolation.integration.test.ts` (MOD) — przepięcie na `fx.createMedia` (usunięcie duplikacji).
- `src/payload-types.ts` (regenerowane, gitignored) — `heroImage` na `Product` i `Variant`.

### Change Log

- 2026-06-23 — S3.2: pola `heroImage` na produkcie i wariancie (`upload → media`, opcjonalne) + autorytatywna walidacja tenant-match (`heroTenantMatch`, R-S3.4) + test integracyjny. Refaktor: wspólny `fx.createMedia` w fixtures (usunięcie duplikacji setupu mediów z S3.1). Korekta: `variants` zagnieżdżone pod `products` w configu pluginu. Pełna suita zielona, lint/types/fallow czyste; `pnpm dev` startuje.

---

## Open Questions (do potwierdzenia przed/po dev)
- **Q1 — `filterOptions` a kontekst tenanta.** Skąd brać tenanta do filtra picker'a: z `data.tenant` edytowanego dokumentu czy z usera (`multiTenantPlugin`)? Zweryfikuj kształt argumentów `filterOptions` w override; `filterOptions` to UX — twardy guard i tak w walidacji (Task 3).
- **Q2 — Gdzie walidacja tenant-match.** Rekomendacja: wspólny `beforeValidate` reużywany przez produkt i wariant (jedno źródło reguły). Alternatywa: field-level `validate` z `req` na każdym `heroImage`. Potwierdź podejście.
- **Q3 — `upload` vs `relationship`.** Rekomendacja: `upload` (picker z podglądem). Potwierdź (jeśli wolisz `relationship`, dostosuj filtr/typy).

---

## Review Findings (2026-06-25 — bmad-code-review, 3-warstwowy)

**Werdykt:** Auditor — wszystkie AC spełnione + testowane (hero produkt+wariant, tenant-match R-S3.4, D6 optional). Czysta historia. Blind/Edge — drobiazgi na write-path (read-side scoping S3.3 jest realnym zabezpieczeniem).

### patch — do naprawy

- [x] [Review][Patch] `idOf('')` → `Number('')===0` → zwraca id `0` zamiast `null` (pole opcjonalne serializowane jako pusty string → fałszywy lookup id 0 → 404). Odrzucaj pusty/whitespace string (i akceptuj tylko całkowite). [src/ecommerce/hero-tenant-match.ts:33-36] (blind+edge) — *współdzielone z S3.4 (idOf używane też dla `categories`)* — ROZWIĄZANE 2026-06-25: `Number.isInteger` (number) + `/^\d+$/` (string trim)

### defer — odłożone / znane

- [x] [Review][Defer] `heroTenantMatch` waliduje hero tylko gdy `heroImage` jest w payloadzie write (partial update / reparenting tenanta nie re-waliduje stałego hero) [src/ecommerce/hero-tenant-match.ts:131-134] — deferred: walidacja write-time + scoping read-side (S3.3 `loadImagesByTenant where{tenant}`, testowane) to faktyczna siatka bezpieczeństwa; pole `tenant` praktycznie immutable

### dismissed

`filterOptions` zwraca `true` dla doc bez tenanta → picker pokazuje media wszystkich (UX-only, hook write jest autorytatywny, brak wycieku do zapisanego doc).
