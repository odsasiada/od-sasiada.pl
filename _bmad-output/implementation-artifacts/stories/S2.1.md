# Story 2.1: Kolekcja `DeliverySlots` (multi-tenant) — okna + cutoff + capacity

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

Jako **dostawca**,
chcę **skonfigurować tygodniowe okna dostawy + cutoff + limit miejsc w dedykowanej, multi-tenantowej kolekcji `DeliverySlots`**,
aby **klient w checkoucie wybierał wyłącznie realne terminy dostawy** (decyzje O1–O4, EPIC-2).

## Acceptance Criteria

1. **Nowa kolekcja `DeliverySlots`** istnieje (`src/collections/DeliverySlots.ts`, `CollectionConfig`, slug **`delivery-slots`**) i jest wpięta do `collections: [...]` w `payload.config.ts` ORAZ do `multiTenantPlugin.collections` (klucz `delivery-slots`) → izolacja tenant gratis (dostawca widzi/edytuje tylko swoje sloty; `platform-admin` widzi wszystkie).
2. **Pola zgodne 1:1 z kontraktem SPIKE-S2** (typ `DeliverySlot` w `src/lib/delivery-slots.ts`):
   - `weekday` — `number`, integer 0–6 (**0 = niedziela … 6 = sobota**, zgodnie z JS `getDay()`),
   - `windowStart` — `text`, wall-clock `HH:mm` (Europe/Warsaw),
   - `windowEnd` — `text`, `HH:mm`,
   - `cutoffDaysBefore` — `number`, integer ≥ 0,
   - `cutoffTime` — `text`, `HH:mm`,
   - `capacity` — `number`, integer ≥ 0;
   - relacja `tenant` dodawana automatycznie przez plugin (**NIE definiować ręcznie**).
3. **Pola i etykiety po polsku** (`label` PL, `admin.description` po polsku). Kod/identyfikatory po angielsku.
4. **Walidacja sensowności przy zapisie** (panel + Local API):
   - `windowEnd > windowStart` (porównanie minut z `HH:mm`),
   - `windowStart` / `windowEnd` / `cutoffTime` w formacie `^\d{2}:\d{2}$` z poprawnym zakresem (00–23:00–59),
   - `weekday` ∈ 0..6 (integer),
   - `cutoffDaysBefore` ≥ 0 (integer),
   - `capacity` ≥ 0 (integer).
5. **`reservedCount` NIE jest polem kolekcji** — obłożenie liczone z `orders` (S2.7). `DeliverySlots` trzyma wyłącznie config.
6. **Brak slotów u tenanta = feature wyłączony (O8)** — kolekcja może być pusta; nie ma żadnego seeda wymuszającego sloty.
7. **`pnpm payload:types` przechodzi** (typ `DeliverySlot` w `src/payload-types.ts`), **`pnpm test` zielone bez regresji** na live Docker Postgres (`od-sasiada-pg`), **`pnpm dev` startuje**.

## Tasks / Subtasks

- [ ] **Task 1 — Utwórz kolekcję `DeliverySlots`** (AC: 1, 2, 3, 4, 5)
  - [ ] Nowy plik `src/collections/DeliverySlots.ts`, eksport `export const DeliverySlots: CollectionConfig`, slug `'delivery-slots'`.
  - [ ] `access`: panel-only, wzór z `src/collections/Media.ts` → `{ create: isAdmin, read: isAdmin, update: isAdmin, delete: isAdmin }`. NIE dodawać własnego `access` po `tenant` — izolację robi plugin (potwierdzone SPIKE-A: własny `access.update` zbędny).
  - [ ] `admin`: `{ group: 'Shop', useAsTitle: ... , defaultColumns: ['weekday','windowStart','windowEnd','capacity'] }` (PL przez `label`/`description`; `useAsTitle` — patrz Open Questions Q1).
  - [ ] Pola dokładnie wg AC#2; NIE definiować pola `tenant` (dodaje plugin), NIE dodawać `reservedCount`.
  - [ ] Walidacje field-level (AC#4) — szczegóły niżej w Dev Notes („Walidacja").
- [ ] **Task 2 — Wepnij kolekcję w config** (AC: 1)
  - [ ] `payload.config.ts`: import `import { DeliverySlots } from '@/collections/DeliverySlots'`; dodaj do `collections: [Users, Tenants, Customers, Media, DeliverySlots]`.
  - [ ] Dodaj `'delivery-slots': {}` do `multiTenantPlugin.collections` (analogicznie do `media: {}`). **Klucz MUSI = slug kolekcji.**
- [ ] **Task 3 — Regeneracja typów + sanity** (AC: 7)
  - [ ] `pnpm payload:types` (potwierdź `DeliverySlot` w `src/payload-types.ts`; NIE edytować pliku ręcznie).
  - [ ] `pnpm dev` — aplikacja wstaje, kolekcja widoczna w panelu w grupie „Shop".
- [ ] **Task 4 — Test integracyjny izolacji + walidacji zapisu** (AC: 1, 4) — *zalecane, wzór: istniejące testy `tests/integration/*.integration.test.ts`*
  - [ ] Utwórz slot dla tenanta A i tenanta B przez Local API (stempluj `tenant` jawnie z konta — akcja serwerowa/test nie auto-populuje tenanta).
  - [ ] Potwierdź: `find` scoped `where { tenant: A }` zwraca tylko slot A (brak wycieku cross-tenant, NFR1).
  - [ ] Potwierdź odrzucenie zapisu łamiącego walidację (`windowEnd <= windowStart`, zły `HH:mm`, ujemny `capacity`).
- [ ] **Task 5 — Weryfikacja końcowa** (AC: 7)
  - [ ] `pnpm test` (live Docker Postgres — patrz Dev Notes „Środowisko") zielone, zero regresji.
  - [ ] `pnpm lint:fix` + `pnpm lint` czyste (Biome).
  - [ ] `fallow audit --format json --quiet` bez nowych blokad przed commitem.

## Dev Notes

### Co to za historia (zakres ścisły)
S2.1 to **wyłącznie warstwa danych**: definicja kolekcji konfiguracyjnej + wpięcie multi-tenant + walidacja zapisu. **Poza zakresem** (świadomie, kolejne historie): UI wyboru slotu (S2.2), wyjątki dni `DeliveryDateExceptions` (S2.8), serwerowa walidacja cutoffu w `placeOrder` (S2.3), capacity/współbieżność (S2.7), snapshot slotu w zamówieniu (S2.4). Czysta funkcja `computeAvailableSlots` **już istnieje** (SPIKE-S2) — NIE pisać jej od nowa.

### Kontrakt kształtu pól — JUŻ ZABLOKOWANY (SPIKE-S2)
Pola muszą odwzorować typ `DeliverySlot` z `src/lib/delivery-slots.ts:29`. To źródło prawdy — nie wymyślaj innych nazw/typów. Mapowanie:

| Pole kolekcji | Typ Payload | Uwagi |
|---|---|---|
| `weekday` | `number` | integer 0–6; **number, NIE string** (locked SPIKE: `computeAvailableSlots` porównuje wprost z `Intl`/`getUTCDay`, zero mapowania w hot-path). Etykiety dni w `admin.description`. |
| `windowStart` | `text` | `HH:mm` wall-clock Europe/Warsaw |
| `windowEnd` | `text` | `HH:mm`; `> windowStart` |
| `cutoffDaysBefore` | `number` | integer ≥ 0; „na jutro" = `1`, same-day = `0` |
| `cutoffTime` | `text` | `HH:mm`; godzina graniczna na dniu `dzień_dostawy − cutoffDaysBefore` |
| `capacity` | `number` | integer ≥ 0; limit aktywnych zamówień na JEDNO wystąpienie slotu |
| `tenant` | (plugin) | **NIE definiować** — dodaje `multiTenantPlugin` |

⚠️ **`reservedCount` ani `id` NIE są polami do zdefiniowania.** `id` jest automatyczne; `reservedCount` to argument wejściowy czystej funkcji liczony z `orders` w S2.7. `DeliverySlots` = tylko config (brak driftu — jedno źródło prawdy obłożenia to zamówienia).

### Decyzja: `weekday` jako `number` (nie `select`)
SPIKE-S2 zablokował reprezentację `number 0–6`. Payload `select` przechowuje wartości jako **string**, co wymusiłoby `Number(weekday)` w ścieżce odczytu i łamie locked-decision „zero mapowania". Użyj `type: 'number'` z `min: 0, max: 6`, walidacją integer i `admin.description: '0 = niedziela, 1 = poniedziałek … 6 = sobota'`. (Ładniejszy dropdown z PL-nazwami to potencjalny custom admin component — Open Question Q1, nie blokujące.)

### Walidacja (AC#4) — field-level `validate`
Payload v3 `validate: (value, { siblingData }) => true | string` (komunikat błędu = string PL). Wzór już w repo: walidacja `postalCode` w `payload.config.ts:104` (regex → string PL).
- Czas `HH:mm`: regex `^([01]\d|2[0-3]):[0-5]\d$` (ostrzejszy niż `^\d{2}:\d{2}$` z compute — łapie 99:99 przy zapisie; czysta funkcja i tak ma własny guard).
- `windowEnd > windowStart`: w `validate` pola `windowEnd` sięgnij po `siblingData.windowStart`, sparsuj oba do minut, porównaj. Komunikat PL: `'Koniec okna musi być późniejszy niż początek.'`
- `number`-y: `min`/`max` + `validate` na integer (`Number.isInteger`) gdzie trzeba; `capacity`/`cutoffDaysBefore` ≥ 0; `weekday` 0–6.
- Komunikaty błędów po polsku (i18n `pl`).

### Wpięcie multi-tenant — wzorzec do naśladowania
`Media` to dokładny precedens: kolekcja niestandardowa (nie-ecommerce) wpięta i w `collections: [...]` (`payload.config.ts:52`) i w `multiTenantPlugin.collections` jako `media: {}` (`payload.config.ts:170`). Powtórz 1:1 dla `delivery-slots`. Plugin stempluje `tenant_id`, ogranicza wiersze w panelu do tenanta admina, `platform-admin` (`userHasAccessToAllTenants`) widzi wszystko. **Kolejność pluginów `[ecommerce, multiTenant]` zostaje bez zmian.**

### Izolacja (NFR1) — 3 warstwy, ale dla S2.1 wystarcza plugin
- **Zapis/panel:** plugin pilnuje tenanta (SPIKE-A potwierdził — własny `access.update` zbędny). W teście/seedzie stempluj `tenant` jawnie (Local API nie auto-populuje tenanta poza kontekstem żądania).
- **Odczyt na froncie (S2.2, nie teraz):** i tak pójdzie przez `overrideAccess: true` + ręczny `where { tenant }` — ale to późniejsza historia.

### Środowisko / workflow (krytyczne)
- **Postgres = Docker** (`od-sasiada-pg`, `postgres:17`, port 5432, baza `od_sasiada`) — **NIE Homebrew**. Jeśli daemon padł: `open -a Docker`, poczekaj aż `docker info` przejdzie, potem `docker start od-sasiada-pg`.
- **Po dodaniu kolekcji ZAWSZE** `pnpm payload:types` (build robi to automatycznie; lokalnie odpal ręcznie). Nie edytuj `src/payload-types.ts` / `src/importMap.js`.
- **Biome** jedyny lint/format: single quotes, bez średników, `organizeImports`/`useSortedKeys` ON — pisz w dowolnej kolejności kluczy i odpal `pnpm lint:fix`.
- **Testy:** integracyjne `tests/integration/*.integration.test.ts` (live DB, `fileParallelism: false`, jedna instancja Payload, timeout 60s); unit `tests/unit/*.test.ts`. Alias `@/` działa w testach.

### Czego NIE robić (anti-patterns)
- ❌ Nie definiuj pola `tenant` ręcznie (duplikat z pluginem).
- ❌ Nie dodawaj `reservedCount`/licznika obłożenia na kolekcji (drift; źródło prawdy = `orders`).
- ❌ Nie rób `weekday` stringiem/`select` (łamie locked-decision SPIKE).
- ❌ Nie filtruj po `status` na `carts` (gotcha `carts.status` — nie dotyczy tej historii, ale pamiętaj przy testach: na `orders` status JEST zapytywalny).
- ❌ Nie pisz `computeAvailableSlots` od nowa — istnieje w `src/lib/delivery-slots.ts`.
- ❌ Nie przenoś logiki capacity/cutoff do tej historii — to S2.3/S2.7.

### Project Structure Notes
- Kolekcje: `src/collections/*.ts`, **PascalCase** plik+eksport (`DeliverySlots.ts` → `export const DeliverySlots`), slug kebab-case (`delivery-slots`). Zgodne z `Tenants.ts`/`Media.ts`/`Customers.ts`.
- Access współdzielony: `src/access/index.ts` (`isAdmin` istnieje — `src/access/index.ts:7`).
- Brak konfliktów ze strukturą. Slug `delivery-slots` nie koliduje z istniejącymi (`users/tenants/customers/media/products/variants/orders/carts/addresses/transactions/variantOptions/variantTypes`).

### References
- [Source: _bmad-output/implementation-artifacts/stories/SPIKE-S2.md#Wynik spike'a (a)] — zablokowany kształt pól, reprezentacje (number/HH:mm/(cutoffDaysBefore,cutoffTime)), `reservedCount` jako wejście funkcji.
- [Source: src/lib/delivery-slots.ts#29] — typ `DeliverySlot` (kontrakt 1:1) + `Weekday`.
- [Source: src/collections/Media.ts] — wzór kolekcji multi-tenant (access panel-only, slug, grupa „Shop").
- [Source: src/payload.config.ts#52,#164] — wpięcie do `collections` + `multiTenantPlugin.collections`; wzór walidacji field-level (`#104`, postalCode).
- [Source: src/access/index.ts#7] — `isAdmin`.
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-2] — O1–O8, kolejność budowania, zależność od SPIKE-S2.
- [Source: _bmad-output/planning-artifacts/architecture.md#2,#5,#7] — model multi-tenant, izolacja NFR1, gotchas.
- [Source: _bmad-output/project-context.md] — reguły TS/Payload/Biome/testów (krytyczne dla agenta).

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List

---

## Open Questions (do potwierdzenia przed/po dev)
- **Q1 — `useAsTitle` / czytelność wiersza w panelu.** Brak naturalnego pola tytułowego (sam `number`/`HH:mm`). Opcje: (a) `useAsTitle: 'windowStart'` (proste), (b) wirtualny `label` składający dzień+okno (`admin.components` / hook) dla UX dostawcy. Rekomendacja: (a) teraz, (b) jako ewentualny polish później. Wpływ tylko na UX panelu, nie na kontrakt danych.
- **Q2 — Test integracyjny w tej historii vs S2.7.** AC#1/#4 da się potwierdzić ręcznie (panel) + jednym małym testem integracyjnym izolacji/walidacji. Pełne testy capacity/współbieżności należą do S2.7. Potwierdź, czy Task 4 ma zostać w S2.1 (zalecane — tani guard regresji NFR1), czy przenieść do S2.7.

_Ultimate context engine analysis completed — comprehensive developer guide created._
