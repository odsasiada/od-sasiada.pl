# Story 3.5: CRUD kategorii w panelu — izolacja tenant (weryfikacja + test)

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

Jako **dostawca**,
chcę **w panelu zarządzać (tworzyć/edytować/usuwać/listować) wyłącznie własnymi kategoriami**,
aby **mieć taksonomię izolowaną od innych dostawców** (NFR1; ryzyko R-S3.4).

## Acceptance Criteria

1. **Dostawca CRUD-uje tylko własne kategorie** — tworzy/edytuje/usuwa kategorie swojego tenanta; nie może modyfikować cudzych.
2. **Dostawca B nie widzi kategorii A** — lista w panelu pokazuje tylko kategorie B; bezpośredni odczyt rekordu A przez Local API z `overrideAccess: false` w kontekście B → pusty/odrzucony (404/brak).
3. **`platform-admin` widzi wszystkie** kategorie wszystkich tenantów (`userHasAccessToAllTenants`).
4. **Izolacja potwierdzona testem integracyjnym** (wzór `orders-isolation`), dołączonym do regresji bezpieczeństwa. Bez regresji: `pnpm test`/`pnpm lint`/`pnpm dev` zielone.

## Tasks / Subtasks

- [ ] **Task 1 — Weryfikacja modelu izolacji** (AC: 1, 2, 3)
  - [ ] Potwierdź, że `Categories` (S3.4) jest w `multiTenantPlugin.collections` (`categories: {}`) → plugin stempluje `tenant`, filtruje wiersze panelu do tenanta admina, `platform-admin` widzi wszystko. To samo działanie co `products`/`media`/`orders` (potwierdzone SPIKE-A: plugin pilnuje, własny `access` po tenant zbędny).
  - [ ] Sprawdź `access` kolekcji `Categories`: panel-only (`isAdmin`) wystarcza; **NIE** dodawać własnego `access` po `tenant` (duplikat z pluginem). Dostrojenie tylko jeśli test wykaże lukę (Q1).
- [ ] **Task 2 — Test integracyjny izolacji** (AC: 1, 2, 3, 4) — *wzór: `tests/integration/orders-isolation.integration.test.ts`*
  - [ ] Setup: kategorie dla tenanta A i B (przez Local API, `tenant` stemplowany jawnie; fixtures `tenantA`/`tenantB`/`adminBUser`).
  - [ ] **Lista/odczyt:** symulując kontekst admina B (`overrideAccess: false`, `user: adminBUser`), `find({ collection: 'categories' })` zwraca tylko kategorie B; `findByID` kategorii A → odrzucone/puste.
  - [ ] **Mutacja:** próba `update`/`delete` kategorii A jako admin B → odrzucona (403/404), analogicznie do `orders-tenant-isolation`/SPIKE-A.
  - [ ] **platform-admin:** kontekst usera z rolą `platform-admin` → `find` zwraca kategorie A i B.
- [ ] **Task 3 — Weryfikacja końcowa** (AC: 4)
  - [ ] `pnpm test` (live Docker Postgres `od-sasiada-pg`) zielone, zero regresji (`orders-isolation`, `order-detail-idor`, `categories-isolation`).
  - [ ] `pnpm lint:fix` + `pnpm lint` czyste; `fallow audit --format json --quiet` bez nowych blokad.

## Dev Notes

### Co to za historia (zakres ścisły)
S3.5 to przede wszystkim **weryfikacja izolacji + test regresyjny**, nie budowa od zera. Izolacja kategorii w panelu jest **out-of-the-box z `multiTenantPlugin`** (jak `products`/`media`/`orders`). Realna praca: napisać dowód (test integracyjny) i — tylko jeśli test wykaże lukę — dostroić `access`. **Poza zakresem:** filtrowanie katalogu po kategorii (S3.6), tworzenie kolekcji (S3.4).

### Dlaczego to działa „za darmo" (kontekst SPIKE-A)
SPIKE-A potwierdził na zamówieniach, że `multiTenantPlugin` pilnuje `update`/widoczności per-tenant — własny `access.update` po tenant okazał się zbędny. Te same gwarancje dotyczą każdej kolekcji wpiętej w `multiTenantPlugin.collections`, więc `categories` dziedziczy izolację panelu. Ta historia po prostu to **udowadnia** dla kategorii (tani guard regresji NFR1).

### Wzór testu izolacji (do naśladowania)
`tests/integration/orders-isolation.integration.test.ts` (z SPIKE-A) jest dokładnym precedensem: tworzy dane dwóch tenantów, symuluje kontekst admina tenanta B (`overrideAccess: false` + `user: adminBUser` z fixtures) i potwierdza brak dostępu cross-tenant (lista pusta / mutacja odrzucona). Fixtures (`tests/setup/fixtures.ts`) dostarczają `tenantA`/`tenantB`/`adminBUser` (`tenant-admin` przypięty do B) oraz `cleanup`.

### Środowisko / workflow
- **Postgres = Docker** (`od-sasiada-pg`) — NIE Homebrew. Daemon padł: `open -a Docker` → `docker info` → `docker start od-sasiada-pg`.
- **Testy:** integracyjne `tests/integration/*.integration.test.ts` (live DB, `fileParallelism:false`, jedna instancja Payload, timeout 60s); alias `@/` działa.
- **Biome** (single quotes, bez średników, `organizeImports` ON) — `pnpm lint:fix`.
- Brak zmian kolekcji/pól → `pnpm payload:types` zwykle niepotrzebne.

### Czego NIE robić (anti-patterns)
- ❌ Nie dodawaj własnego `access` po `tenant` „na wszelki wypadek" — plugin pilnuje; ręczny duplikat może wejść w konflikt (SPIKE-A). Dostrajaj tylko gdy test wykaże lukę.
- ❌ Nie używaj `overrideAccess: true` w teście izolacji (to obchodzi access — testuj z `overrideAccess: false` + `user`).
- ❌ Nie zmieniaj kolekcji `Categories` (to S3.4) ani nie dotykaj filtrowania katalogu (S3.6).
- ❌ Nie filtruj `carts` po `status` (gotcha — nie dotyczy, ale pamiętaj o wzorcach zapytań).

### Project Structure Notes
- NEW: `tests/integration/categories-isolation.integration.test.ts`. Ewentualne dostrojenie: `src/collections/Categories.ts` (`access`) — tylko jeśli test wykaże lukę.
- **Zależność:** S3.4 (kolekcja `Categories`). Następnik: S3.6 (filtr). Build order: … → S3.4 → **S3.5** → S3.6.

### References
- [Source: tests/integration/orders-isolation.integration.test.ts] — wzór testu izolacji tenant (symulacja kontekstu admina B).
- [Source: tests/setup/fixtures.ts#39,#154] — `tenantA`/`tenantB`, `adminBUser` (`tenant-admin`), `cleanup`.
- [Source: src/payload.config.ts#170,#180] — `multiTenantPlugin.collections` (`categories: {}`), `userHasAccessToAllTenants` (`platform-admin`).
- [Source: src/collections/Categories.ts (S3.4)] — kolekcja będąca przedmiotem weryfikacji (access panel-only).
- [Source: _bmad-output/implementation-artifacts/stories/SPIKE-A.md (EPIC-1)] — dowód, że plugin pilnuje izolacji `update` (własny access zbędny).
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-3] — D5 (kategorie per-tenant), zależność S3.4.
- [Source: _bmad-output/project-context.md] — izolacja NFR1, testy bezpieczeństwa obowiązkowe, reguły testów/Biome.

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List

---

## Open Questions (do potwierdzenia przed/po dev)
- **Q1 — Czy potrzebne dostrojenie `access`.** Rekomendacja: zacznij od samego pluginu (jak `products`/`orders`); dodaj custom `access` TYLKO jeśli test izolacji wykaże lukę. Potwierdź, że nie chcesz prewencyjnie dokładać własnych reguł (ryzyko konfliktu z pluginem, SPIKE-A).
- **Q2 — Zakres testu mutacji.** Rekomendacja: pokryć read (lista/findByID) + update + delete cross-tenant (pełny CRUD-isolation). Potwierdź, czy delete też ma być w teście (zalecane).
