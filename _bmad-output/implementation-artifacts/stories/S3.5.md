# S3.5 — CRUD kategorii w panelu (tenant-scoped)

Status: ☐ TODO · ready-for-dev · EPIC-3 · utworzono 2026-06-21

## Historyjka

Jako **dostawca** chcę **zarządzać tylko własnymi kategoriami w panelu**, aby **mieć izolowaną taksonomię**.

## Kryteria akceptacji (z sprint-3.md)

- Dostawca tworzy/edytuje/usuwa **tylko własne** kategorie.
- Dostawca B nie widzi kategorii dostawcy A (lista + bezpośredni dostęp → puste/404).
- `platform-admin` widzi wszystkie.

## Zależności

- S3.4.

## Uwagi implementacyjne

- Izolacja głównie out-of-the-box z `multiTenantPlugin` (jak `products`/`media`); ta historia = **weryfikacja** izolacji + ewentualne dostrojenie `access`.
- Wzór testu izolacji: `tests/integration/orders-isolation.integration.test.ts`.

## Plan weryfikacji

- Test izolacji: admin-dostawca B nie listuje ani nie czyta kategorii A; `platform-admin` widzi wszystkie.
