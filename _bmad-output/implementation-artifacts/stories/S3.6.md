---
baseline_commit: 3dbf7cfb5db456958775df4d9e20bda0d5f2efd6
---

# Story 3.6: Filtr katalogu po kategorii (server-side, tenant-scoped)

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

Jako **klient**,
chcę **filtrować katalog `/[tenant]` po kategorii (`?kategoria=<slug>`), wyliczany serwerowo**,
aby **szybciej znaleźć produkty** (decyzja D4; ryzyko R-S3.5 — przeciek cross-tenant przez filtr).

## Acceptance Criteria

1. **Filtr server-side w `getCatalog`** — `/[tenant]?kategoria=<slug>` zawęża listę do produktów tenanta w danej kategorii. `where` zawsze zawiera `tenant` ORAZ `_status: 'published'` PLUS dopasowanie kategorii w relacji `hasMany`.
2. **Produkt w wielu kategoriach pojawia się w KAŻDEJ** ze swoich kategorii (D4) — wynika z dopasowania „relacja zawiera kategorię".
3. **Bez parametru `?kategoria=` → wszystkie** opublikowane produkty tenanta (zachowane obecne zachowanie katalogu).
4. **Lista kategorii w UI tylko z tego tenanta** (`where { tenant }`) — linki/filtry pokazują wyłącznie kategorie dostawcy.
5. **Izolacja (R-S3.5):** nieznany/cudzy slug → wynik **pusty/zignorowany**, **NIGDY** cross-tenant. Rozwiązanie slug→kategoria jest tenant-scoped (`where { slug, tenant }`).
6. **Bez regresji** — render zdjęć (S3.3), koszyk, paginacja działają; `pnpm lint`/`pnpm test`/`pnpm dev` zielone.

## Tasks / Subtasks

- [x] **Task 1 — Lista kategorii tenanta (UI)** (AC: 4, 5)
  - [x] `src/lib/shop.ts`: `getCategories(tenantId): Promise<{ id; name; slug }[]>` — `payload.find({ collection: 'categories', overrideAccess: true, where: { tenant: { equals: tenantId } } })`. Mapuj minimalnie (name + slug).
- [x] **Task 2 — Filtr w `getCatalog`** (AC: 1, 2, 3, 5)
  - [x] Rozszerz `getCatalog(tenantId: number, categorySlug?: string)`. Bez `categorySlug` → obecne zachowanie (wszystkie published tenanta).
  - [x] Z `categorySlug`: najpierw rozwiąż kategorię **tenant-scoped** — `payload.find({ collection: 'categories', where: { and: [{ slug: { equals: categorySlug } }, { tenant: { equals: tenantId } }] }, limit: 1 })`. Brak dopasowania → zwróć `[]` (pusto; Q1).
  - [x] Dopasowanie produktów: dołóż do `where` produktów warunek relacji `hasMany`, np. `{ categories: { in: [categoryId] } }` (zweryfikuj operator dla relacji hasMany — `in`/`contains` — Q2). `where` MUSI nadal mieć `tenant` + `_status: 'published'` (R-S3.5).
  - [x] Zachowaj dotychczasowe wzbogacanie o `image` (S3.3) i warianty.
- [x] **Task 3 — UI filtra na stronie katalogu** (AC: 1, 3, 4)
  - [x] `src/app/(frontend)/[tenant]/page.tsx` (server): odczytaj `searchParams.kategoria` (Next 16 — `searchParams` jest Promise, `await`). Pobierz `getCategories(tenant.id)` + `getCatalog(tenant.id, kategoria)`.
  - [x] Renderuj nawigację filtra: linki `?kategoria=<slug>` + „Wszystkie" (czyści filtr). Server-rendered linki (bez stanu klienckiego) — wybór = nawigacja + re-render. Zaznacz aktywną kategorię.
- [x] **Task 4 — Test integracyjny** (AC: 1, 2, 5) — *wzór: `tests/integration/*.integration.test.ts`*
  - [x] Produkt A1 w kategoriach K1,K2 (tenant A); produkt A2 w K2. Filtr `K1` → tylko A1; `K2` → A1 i A2 (wielokategoryjny w każdej — D4).
  - [x] Bez slugu → wszystkie published A.
  - [x] **Cross-tenant:** slug kategorii tenanta B podany dla tenanta A → **pusto** (nie produkty B, nie produkty A); nieznany slug → pusto. Nigdy cross-tenant (R-S3.5).
- [x] **Task 5 — Weryfikacja końcowa** (AC: 6)
  - [x] `pnpm test` zielone, zero regresji (`orders-isolation`, `categories-isolation`); `pnpm lint:fix` + `pnpm lint` czyste; `fallow audit --format json --quiet` bez nowych blokad.
  - [x] `pnpm dev`: filtr działa, produkt wielokategoryjny widoczny w każdej kategorii, zdjęcia (S3.3) i koszyk działają.

## Dev Notes

### Co to za historia (zakres ścisły)
S3.6 = **server-side filtrowanie katalogu po kategorii** + UI filtra. Domyka EPIC-3. **Poza zakresem:** tworzenie kolekcji/relacji (S3.4), izolacja CRUD panelu (S3.5), render zdjęć (S3.3 — tu tylko nie zepsuć).

### R-S3.5 (KLUCZOWE) — `tenant` ZAWSZE w `where`
`getCatalog` czyta przez `overrideAccess: true` (plugin nie pilnuje publicznych zapytań — izolacja jest RĘCZNA). Filtr kategorii to nowy wektor przecieku: gdyby dopasowanie kategorii pominęło `tenant`, klient mógłby zobaczyć produkty innego dostawcy. **Dwa miejsca muszą być tenant-scoped:** (a) rozwiązanie `slug→categoryId` (`where { slug, tenant }`), (b) zapytanie produktów (`where { tenant, _status, categories }`). Wzór: istniejący `getCatalog` (`shop.ts:38`) już trzyma `tenant`+`_status` — tylko dołóż warunek kategorii, nie luzuj reszty.

### Dlaczego rozwiązywać slug w obrębie tenanta
Sluggi kategorii nie są globalnie unikalne (S3.4: unikalność per-tenant nie wymuszona). Gdyby filtrować produkty wprost po slug bez rozwiązania w obrębie tenanta, slug tenanta B mógłby przypadkiem trafić. Rozwiązanie `slug→id` z `where { slug, tenant }` gwarantuje, że bierzemy kategorię TEGO dostawcy; nieznany/cudzy slug nie rozwiąże się → pusto (R-S3.5).

### Zapytanie relacji `hasMany`
Produkt↔kategoria to `relationship hasMany` (S3.4). Filtrowanie „produkty, których `categories` zawiera X" w Payload zwykle: `where: { categories: { in: [categoryId] } }`. **Zweryfikuj operator** dla relacji hasMany w zainstalowanej wersji (`in` vs `contains`) — Q2. Produkt z wieloma kategoriami naturalnie spełnia warunek dla każdej z nich (D4).

### Next 16 — `searchParams` async
W Next 16 (App Router, ten projekt) `params` i `searchParams` są **Promise** — `await` je w server component (jak już robione z `params` w `page.tsx`). `?kategoria=` → string | undefined.

### Render server-side, bez stanu klienckiego
Filtr to linki nawigacyjne (`<Link href="?kategoria=slug">`), nie client state — wybór kategorii = nawigacja + serwerowy re-render `getCatalog`. Nie potrzeba 'use client'; `Catalog.tsx` (client) dostaje już przefiltrowaną listę propsami. Granica hydratacji bez zmian.

### Stan plików (UPDATE)
- **`src/lib/shop.ts`**: `getCatalog` (`#38`) — dodaj opcjonalny `categorySlug` + filtr; nowy `getCategories`. Zachowaj `tenant`+`_status` w `where` oraz wzbogacanie `image` (S3.3).
- **`src/app/(frontend)/[tenant]/page.tsx`**: dodaj odczyt `searchParams`, listę kategorii, UI filtra. Zachowaj sekcję intro/`Catalog`.

### Środowisko / workflow
- **Postgres = Docker** (`od-sasiada-pg`) — NIE Homebrew.
- **Biome** (single quotes, bez średników, `organizeImports`/`useSortedKeys`/`useSortedAttributes` ON) — `pnpm lint:fix`.
- **Trasy/parametry URL po polsku** (`?kategoria=`) — zgodne z konwencją (`koszyk`, `moje-zamowienia`).
- **Testy:** integracyjne `tests/integration/*` (live DB, `fileParallelism:false`).

### Czego NIE robić (anti-patterns)
- ❌ NIGDY nie pomijaj `tenant` w `where` filtra ani w rozwiązaniu slug (R-S3.5 — przeciek cross-tenant).
- ❌ Nie filtruj produktów wprost po slug bez rozwiązania `slug→id` tenant-scoped (slug nie jest globalnie unikalny).
- ❌ Nie rób filtrowania po stronie klienta (server-side w `getCatalog` — AC1).
- ❌ Nie gub `_status: 'published'` ani wzbogacania `image` (S3.3) przy dokładaniu filtra.
- ❌ Nieznany/cudzy slug nie może „po cichu" pokazać wszystkich cudzych — pusto/ignorowane, nigdy cross-tenant.

### Project Structure Notes
- Modyfikacje: `src/lib/shop.ts` (`getCatalog` + `getCategories`), `src/app/(frontend)/[tenant]/page.tsx` (UI filtra). Test: `tests/integration/catalog-category-filter.integration.test.ts`.
- **Zależności:** S3.4 (kategorie + `slug` + relacja `hasMany`), S3.3 (render katalogu — nie zepsuć). **Ostatnia historia EPIC-3.** Build order: … → S3.4 → S3.5 → **S3.6**.

### References
- [Source: src/lib/shop.ts#38] — `getCatalog` (gdzie dodać `categorySlug` + filtr; wzór `where { tenant, _status }`); dodać `getCategories`.
- [Source: src/app/(frontend)/[tenant]/page.tsx] — server component katalogu (gdzie odczytać `searchParams` i renderować filtr).
- [Source: src/collections/Categories.ts (S3.4)] — pole `slug` (klucz filtra), relacja `hasMany` na produkcie.
- [Source: tests/integration/orders-isolation.integration.test.ts] — wzór testu izolacji (cross-tenant pusto).
- [Source: _bmad-output/implementation-artifacts/stories/S3.4.md] — `slug` + relacja `hasMany`; [S3.3.md] — wzbogacanie `image` w `getCatalog` (nie zepsuć).
- [Source: _bmad-output/planning-artifacts/epics.md#EPIC-3] — D4 (produkt w wielu kategoriach widoczny w każdej), zależności S3.4/S3.3.
- [Source: _bmad-output/project-context.md] — izolacja NFR1/R-S3.5 (ręczny `where { tenant }` na publicznych odczytach), trasy PL, reguły testów/Biome.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Claude Opus 4.8)

### Debug Log References

- RED: nowy `catalog-category-filter.integration.test.ts` → 4 failed (getCatalog ignorował slug; getCategories nie istniał). GREEN po implementacji → 6/6.
- Q2 zweryfikowane: operator `where { categories: { in: [categoryId] } }` poprawnie dopasowuje relację hasMany w `payload@^3.85.1` (test AC2 — produkt wielokategoryjny widoczny w każdej kategorii, D4).
- Smoke `pnpm dev` (serwer na :3000): `/swieze-z-kaszub` → HTTP 200; `/swieze-z-kaszub?kategoria=<nieznany>` → HTTP 200 (pusta lista, Q1), nav filtra z linkami `?kategoria=` wyrenderowany, brak błędów runtime.

### Completion Notes List

- ✅ **Task 1:** `getCategories(tenantId)` w `shop.ts` — tenant-scoped (`overrideAccess: true` + ręczny `where { tenant }`, R-S3.5), sort po nazwie, mapowanie minimalne (`id/name/slug`), nowy typ `ShopCategory`.
- ✅ **Task 2:** `getCatalog(tenantId, categorySlug?)` — rozwiązanie `slug→id` tenant-scoped (`where { and: [slug, tenant] }`); nieznany/cudzy slug → `[]` (Q1, R-S3.5). Filtr produktów dokładany warunkowo `categories: { in: [categoryId] }` przy zachowaniu `tenant` + `_status: 'published'` oraz całego wzbogacania `image` (S3.3) i wariantów.
- ✅ **Task 3:** `searchParams.kategoria` (Next 16 — `await`) na `[tenant]/page.tsx`; równoległe `getCategories`+`getCatalog` (`Promise.all`). Filtr wydzielony do serwerowego komponentu `CategoryFilter` (linki `?kategoria=<slug>` + „Wszystkie", aktywna podświetlona przez `is-active`/`aria-current`; bez stanu klienckiego). Style `.category-filter` w `globals.css`.
- ✅ **Task 4:** `catalog-category-filter.integration.test.ts` (6 testów): AC4 getCategories tenant-scoped, AC3 bez slugu→wszystkie, AC1 K1→tylko A1, AC2 K2→A1+A2 (D4), AC5 cross-tenant slug→pusto, AC5 nieznany slug→pusto.
- ✅ **Task 5:** `pnpm test` 128 passed / 1 skipped (0 regresji, +6 nowych). `pnpm lint` — moje pliki czyste (pozostałe 5 warningów pre-existing: `categories-isolation`, `tenant-cascade-delete`). `tsc --noEmit` bez błędów w moich plikach. `fallow audit` — brak NOWYCH blokad (complexity 6→5 po wydzieleniu `CategoryFilter`; duplicates bez zmian = 3; werdykt `fail` dziedziczony z odroczonych dup/complexity per [[fallow-notes]]).
- ℹ️ **Q1** rozstrzygnięte: nieznany/cudzy slug → **pusto** (`[]`), bezpieczniejsze i jednoznaczne. **Q2:** operator `in`. **Q3:** pasek linków + „Wszystkie", aktywna podświetlona, bez licznika (MVP).

### File List

- `src/lib/shop.ts` (MOD — `getCategories` + `ShopCategory`; `getCatalog` z opcjonalnym `categorySlug` + filtr tenant-scoped)
- `src/app/(frontend)/[tenant]/page.tsx` (MOD — `searchParams.kategoria`, `Promise.all`, render `CategoryFilter`)
- `src/components/shop/CategoryFilter.tsx` (NEW — serwerowy komponent nawigacji filtra)
- `src/app/(frontend)/globals.css` (MOD — style `.category-filter`)
- `tests/integration/catalog-category-filter.integration.test.ts` (NEW — regresja filtra + izolacja cross-tenant)

## Change Log

- 2026-06-25 — S3.6 zaimplementowane (filtr katalogu po kategorii, server-side, tenant-scoped). `getCategories` + filtr w `getCatalog`, UI `CategoryFilter`, test regresyjny izolacji (cross-tenant pusto). Status → review.

---

## Review Findings (2026-06-25 — bmad-code-review S3.6)

**Werdykt warstw:** Acceptance Auditor — wszystkie 6 AC spełnione, izolacja R-S3.5 poprawna na wszystkich 3 zapytaniach (slug→id, produkty, lista kategorii), testy pokrywają AC1–AC5 (w tym realny cross-tenant). Blind Hunter — brak dziur izolacji. Edge Case Hunter — najpoważniejsze: array-typed `searchParams` + niedeterminizm przy duplikacie sluga.

### patch — do naprawy

- [x] [Review][Patch] Powtórzony `?kategoria=a&kategoria=b` → Next 16 zwraca `string[]`, typ deklaruje `string`; trafia do `getCatalog`/Payload `equals` (undefined behavior → pusta strona sklepu). Znormalizuj `kategoria` do pojedynczego stringa. [src/app/(frontend)/[tenant]/page.tsx:19,29] (blind+edge) — ROZWIĄZANE 2026-06-25: typ `string | string[]` + `Array.isArray(...) ? [0] : ...`

### defer — odłożone, znane lub poza zakresem

- [x] [Review][Defer] Duplikat sluga w obrębie tenanta → `limit:1` rozwiązuje niedeterministycznie + dwa identyczne linki podświetlone jako aktywne [src/lib/shop.ts; src/collections/Categories.ts] — deferred, znane Q1/S3.4 (unikalność sluga per-tenant niewymuszona, już odłożone w review S3.5)
- [x] [Review][Defer] `getCategories` `limit: 200` cicho ucina pasek filtra przy >200 kategoriach [src/lib/shop.ts] — deferred, nowe ale low (spójne z innymi twardymi limitami; >200 kategorii nierealne w MVP)
- [x] [Review][Defer] Twarde limity katalogu — >500 produktów / >1000 wariantów cicho ucinane [src/lib/shop.ts] — deferred, pre-existing (nie wprowadzone przez S3.6)
- [x] [Review][Defer] Brak `sort` na zapytaniu produktów → kolejność z bazy (filtered vs unfiltered może się różnić) [src/lib/shop.ts] — deferred, pre-existing

### dismissed (10) — szum / FP / by-design

`overrideAccess: true` na publicznym odczycie (wzorzec NFR1 wymagany przez project-context); pusty/whitespace slug → pusto (spójne z Q1); `Promise.all` sprzęga fetch kategorii z katalogiem (zgodne z dotychczasowym all-or-nothing render); AC1 test exact-match bez sortowania (1 element — bezpieczne); `collection as never` w cleanup testu (test-only, wzorzec z `categories-isolation`); brak `_status` guard na kategorii (Categories bez wersji/draftów — inertne); „Wszystkie" gubi inne query-paramy (brak paginacji query-param dziś); `getCategories` czyta gdy lista pusta (zamierzone — pasek musi renderować kategorie tenanta).

---

## Open Questions (do potwierdzenia przed/po dev)
- **Q1 — Nieznany/cudzy slug: pusto czy ignorować filtr.** AC dopuszcza oba („pusto/ignorowana"). Rekomendacja: **pusto** (jawne „brak produktów w tej kategorii") — bezpieczniejsze i mniej mylące niż ciche pokazanie wszystkich. Potwierdź.
- **Q2 — Operator zapytania relacji `hasMany`.** Rekomendacja: `where { categories: { in: [categoryId] } }`. Zweryfikuj w `payload@^3.85.1`, czy `in`/`contains` poprawnie dopasowuje relację hasMany (produkt z wieloma kategoriami w każdej z nich — D4).
- **Q3 — UI filtra.** Rekomendacja: pasek linków kategorii + „Wszystkie", aktywna podświetlona; bez stanu klienckiego (nawigacja query-param). Potwierdź zakres UI (czy ma być licznik produktów per kategoria — raczej nie w MVP).
